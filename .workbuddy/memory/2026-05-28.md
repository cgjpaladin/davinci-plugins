# 2026-05-28

## 审查模式 + 导出 + ffmpeg 打包

**审查模式改进**：
- 入口：单击缩略图（原双击）+ 空格键（有选中文件时打开第一个）
- × 关闭按钮移到 titlebar 左侧
- SH 编辑加「点击外部提交」提示
- 状态栏 O/P/N 快捷键已移除

**导出 Excel**：
- 改用 openpyxl（手拼 XML 在 macOS 不兼容）
- 10 列：缩略图/EP/SC/SH/TK/描述/类型/作者/V/状态/文件名
- 缩略图按实际比例嵌入，行高自动匹配
- 补零存为文本（zfill 不掉零）
- 扩展名从实际文件读取
- 原生保存对话框（pywebview SAVE_DIALOG）
- build.sh 加 `--hidden-import openpyxl`

**ffmpeg/ffprobe 打包**：
- `--add-binary` 传入 app bundle
- Python 端优先用 bundle 内二进制
- 解决用户没装 ffmpeg 导致缩略图和元数据缺失

**指引补全**：
- 缩略图 hover "单击预览"
- 空状态③ "单击缩略图预览视频"
- SH 编辑器："+ 添加 · − 删除 · ENTER 提交 · ESC 取消 · 点击外部提交"
- 导出按钮 tooltip、OK/KP/NG tooltip

**修复**：AIVID fillMax 8→7、renderList(true) 审查三处、inlineStr→openpyxl、图片比例、死代码清理

**视频播放方案变更**：bottle HTTP → file:// URL → JS API Blob URL（base64 直传，绕过 WKWebView 限制）

**交互细节**：Prev/Next 原地替换不关弹窗、点击视频画面播放/暂停、JKL 穿梭、, . 逐帧、← → 改为 2 秒

**分发**：build.sh 加 codesign 去除 Gatekeeper 隔离，传源码 zip 解决 Intel/ARM 兼容

## 原版导表功能同步

**5 文件改动**（v3.3→v3.4）：
- `renamer_web.py`：+89 行，`export_table()` + `save_file()` 方法。列头 Ep/Sc/Gr/Tk/描述/制作方式/制作者/v/通过情况/文件名，对应原版 9 字段
- `app_table.js`：+18 行，`exportExcel()` 函数复用 `buildTK(i)` 计算 tk
- `renamer_table.html`：+1 行导出按钮（位置：撤销按钮右侧）
- `app_table.css`：+8 行 `.hero-export` 样式（与 undo 对齐）
- `build.sh`：+5 行 `--hidden-import`（PIL/openpyxl）确保 PyInstaller 打包

**关键适配点**：
- 原版字段：ep/sc/gr/tk/desc/method/author/ver/status（创壹版有 shot/type 无语 method/gr）
- NUM_KEYS zfill：ep/sc/gr/tk/ver（5 个数位字段）
- 文件名前缀：Ep/Sc/Gr/Tk/v（创壹版全大写）
- 列头"制作方式"替代创壹版的"类型"
- buildTK 已存在于原版，导出直接复用

**硬编码消除重构**（同一轮，替换了初版 116 行中的硬编码）：
- Python `export_table`：HEADERS/KEYS/WIDTHS/NUM_KEYS 四份并行列表 → 单一 COLUMNS 结构化列表（含 header/key/width/zfill）
- `_PFX` 从 FIELD_CONFIG `name` 字段推导（不再手写 `{'ep':'Ep',...}`）
- `_FIELDS` 从 FIELD_CONFIG 顺序推导（不再手写 `('ep','sc',...)`）
- 缩略图尺寸 72/60 提为 THUMB_W/THUMB_H_MAX 常量
- JS `exportExcel`：字段列表从 `window._fieldKeysAll` 动态迭代（不再手写 `{ep:ff.ep||'',...}`）
- 默认文件名前缀提为 `EXPORT_FILENAME_PREFIX` 常量

**效果**：新增字段现在只需改 FIELD_CONFIG + COLUMNS（新增 1 行 dict），JS 端完全不需动

## 原版深度审计 + 10 项修复

**先验证再修**：审计 16 条中 3 条被推翻（desc 下划线是故意清洗、buildTK 不含 status 是正确设计、HTML 版本号降级），实际修了 10 项：

| # | 修复 | 文件 | 说明 |
|:--|------|------|------|
| 1 | version_info.txt 3.3.0→3.4.0 | version_info.txt | 3 处 |
| 2 | 补 `--hidden-import openpyxl.utils.units` | build.sh | 打包后行高不降级 |
| 3 | _splice.py 正则替换版本号 | _splice.py | 不再耦合精确字符串 |
| 4 | 移除 `echo()` 死代码 | renamer_web.py | - |
| 5 | 移除 `normalize_path()` 死代码 | renamer_web.py | - |
| 6 | 移除 `loadThumbsEx()` 死代码 | app_table.js | - |
| 7 | 移除 `descLocked` 死变量 | app_table.js | 声明+赋值+自测 3 处 |
| 8 | `updCount()` 硬编码→动态 | app_table.js | `_fieldKeysAll.every()` |
| 9 | `fillCount/ready` 6 处硬编码→动态 | app_table.js | `nFields` + `fieldKeys` |
| 10 | HTML 版本号动态化 | renamer_table.html + app_table.js | `versionLabel` span + JS 注入 |

**效果**：新增/删字段只需改 FIELD_CONFIG，JS 端所有就绪检查、完成度色条、缺失提示、就绪计数全部自动跟随

- 审查面板 method-desc 联动完全对齐表格模式：locked/dropdown(text)/text 三种模式 + _buildDesc 统一重建
- desc placeholder 增加 CSS visibility（#555 颜色）+ 空 method hint 对齐表格"请先选择制作方式"
- _buildDesc 始终创建新 div（修复 replaceWith 在 WKWebView 下旧元素残留 bug）
- 删 Ctrl+Enter 绑定 + 状态栏文字（两版）
- 删截图/全屏按钮（两版）
- 审查面板 ep/sc/gr/ver 合并为一行 4 列 grid
- Tab/Enter 审查字段切换 + Home/End 跳视频首尾（两版）

## 待办：原版加审查面板

明天从创壹版移植审查模式到原版：
- Python: `get_media_info` / `get_media_data` / `get_codec`
- HTML/CSS/JS: 审查面板 overlay + 视频播放 + 字段编辑 + JKL + 逐帧
- build.sh: `--add-binary ffmpeg/ffprobe`
- 字段差异: `gr` 代 `shot`、`method` 代 `type`、desc 无 AIVID 锁定

