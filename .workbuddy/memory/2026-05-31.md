# 2026-05-31

## 核心工作：一键更新系统 + 全面解耦

### 商业化决策（下半场）
- **定价**：¥99 买断（个人）/ ¥648 企业定制
- **试用**：30 天 → 到期后强制购买（Downie 模式）
- **销售渠道**：发货100 自动发卡（主）→ 快发卡（备）。接受跳转
- **后端**：腾讯云 SCF（主）→ 阿里云 FC（备）。无备案域名问题
- **License 架构**：Downie 风格——HMAC-SHA256（纯标准库）+ 服务端权威校验，不做 RSA
- **激活码**：手动生成 + 发货100 自动发放；支持赠送 Key
- **多产品**：一个码解锁多个产品（Adobe 模式），终身买断
- **防破解**：机器指纹 + 月度心跳 + 连续失败自动吊销
- **崩溃上报**：一键上传，待设计
- **豆包调研中**：Downie License 深度逆向分析 + 简化方案

### 更新系统
- 实现完整一键更新：检测→多链路下载→解压→安装→重启
- 版本检查多链路：jsDelivr CDN → GitHub API → GHProxy
- 下载多链路：同上述三条 + SHA256 校验
- 安装流程：`osascript do shell script` → `/bin/bash install_update.command --update` → 直接 root 覆盖
- GitHub API base64 解码支持（api.github.com 在国内可用）
- 全 ASCII 文件名（zip 根目录、URL、.command 文件名）

### 配置解耦
- 新建 `shared/update_config.py`：仓库名、URL 多链路、超时、最小下载尺寸 → 统一入口
- `shared/updater.py` 重写：多链路回退 + GitHub API base64 解码
- `tools/publish_release.sh`：一键发布（上传 zip + version.json + CDN 刷新）

### UI 重构
- `_do_update` 函数重写：多链路下载 + sha256 校验 + 非嵌套 osascript + 错误精确捕获
- 60 处 UI 参数常量化：FONT_*/SPACE_*/SIZE_*/PAD_*/RAD_*/STYLE_* 设计参数块
- 更新按钮状态：无更新灰显「✓ 最新」不可点击，有更新金色高亮

### 构建系统
- `build_personal.sh --all`：一次性出全量(67MB) + 增量(504KB)两个包
- `build_personal.sh --update`：增量包用 ASCII 根目录名 `davinci_plugin_update/`
- `install.command --update`：跳过 Python/凭证输入，直接 root 安装，扫描 ui.py+shared/ 找源码

### 分发四模式
- 本地 dev：python3 ui.py 直连达芬奇
- SMB 公司：/Volumes/MYJC/ 同步
- 个人全量(67MB)：新用户首次安装含 Python pkg
- 个人增量(504KB)：已装用户点更新按钮

### 踩坑
- 中文文件名：zip/URL/GitHub Release 三处乱码，全部改 ASCII
- jsDelivr 24h 缓存：发版后需手动 purge
- osascript `do shell script` 默认 /bin/sh 不读 shebang
- Python 相对导入 `from .update_config` 在达芬奇非包路径失效
- GitHub Release 中文文件名自动变 `_.zip`
