## 2026-06-08

- **归档文件夹去制作方式**：`build_folder` compound 从 `EP{ep}_SC{sc}_{method}_v{ver}` → `EP{ep}_SC{sc}_v{ver}`
- **TK 正则修复**：`re.search(r'\bTk...')` → `r'_Tk...'`。`_` 是 `\w` 字符，`\b` 从不匹配 `_Tk`，TK 扫描始终归零
- 版本号 3.6 → 3.6.1，4 文件同步
- DMG 打包 v3.6.1（55MB）
- 批量命名工具开发 skill 同步更新
