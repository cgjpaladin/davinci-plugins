
## 个人版安装流程审计

- shell_personal.py 缺 `-B` + `PYTHONUTF8=1` + `WORKBUDDY_PERSONAL=1` → 已补
- 构建→解压→bash语法→模拟安装 4步验证全部通过
- zip 含 44 文件：shell/launcher/ui/shared(16py)/dicts(8)/.env.example/install_update.command
- install_update.command 的 .env/dicts 备份恢复机制确认有效
- install.command 已在 git 中正确跟踪
- 出厂检验通过：2.5.0

## Keychain 安全存储

- 新增 `shared/secure_store.py`：macOS 用 Keychain，其他用 chmod 600 文件
- `_load_api_keys` / `_save_api_keys` 改成 Keychain 读写，零明文落盘
- 启动时从 Keychain 注入 `DEEPSEEK_API_KEY` / `FEISHU_BOT_APP_ID` / `FEISHU_BOT_APP_SECRET` 到 os.environ
- `.env` → api_keys.json → Keychain 三级迁移自动完成
- 配置页保存后 Keychain 立刻生效（重启不丢 key）
- build_personal.sh 已加入 secure_store.py 打包
- `platform` 模块在 shared/ 有同名文件冲突 → 改用 `sys.platform`
