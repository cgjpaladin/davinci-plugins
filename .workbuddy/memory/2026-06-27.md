# 2026-06-27

## 黑边算法重构（19:55—20:16）
- 裁缝老师发现：时间线蓝色标记处有明显黑边，工具未检出
- API 全量排查：3 个蓝色标记（帧 2015/2249/2355）+ 1 个红色标记（帧 2420）+ 1 个紫色标记（帧 2451）
- V1+V2 双层轨道场景：V2 去字幕版覆盖 V1 的 2416-2452（36 帧），V1 总长 57 帧
- 旧算法 80% 覆盖阈值判定正确但时间码取片段头 → 剪辑师跑到错误位置
- 重写：`_exposed_ranges()` 区间减法替代 80% 阈值，暴露帧精准时间码
- 红色标记处原报 01:36:16 → 改为 01:38:02（21帧），即 V2 结束后的暴露区间
- 裁缝老师纠错：#38 误猜腾讯云SCF后端（实际只有阿里云FC）、#39 误判误报（实为时间码偏移）

## 分发体系终局（06-20 遗留）
- install.command 引用从「请勿直接解压此文件.zip」→ data.zip，重建全量+增量包
- license_fc.js 激活指纹加 .trim()，catch 加日志

## 复盘 v8
- 日报 + MEMORY + delivery-checker agent 更新
- consolidation-2026-06-27 tag

## AI去字幕紧急排障（18:21—20:20）
- 白岳/陈佳馨/赵梓言/王平波/袁宁/张冀恒 报告插件崩溃或卡死
- 根因：stderr 回环死循环 — `_ui_write_direct` 用 `print(..., file=sys.stderr)` 写 `_UIStderr`，`_UIStderr.write` 调 `_ui_write` → `_ui_write_direct` → 无限递归
- 任何含 `❌⚠失败Error` 的消息都触发，OSS 超时/锁冲突/余额不足 全部中招
- 修复 6 个 bug：
  1. `ui_widgets.py:` `_real_stderr = sys.stderr` 在重定向前保存，所有 stderr 写改用 `_real_stderr.write()`
  2. `pipeline_log.py:` `StepLogger.fail/warn` 删 `print(file=sys.stderr)`（双写+回环入口）
  3. `ui_widgets.py:` `_flush_log` 队列消费走 `_write_to_te` 折叠而非裸 `te.Append`
  4. `ui_pipeline.py:` 补 `ADAPTER_CONFIGS` import（引擎切换报告时 NameError）
  5. 按钮状态：`_set_btn` 加 undo/color 参数，`process()` finally 不放回 scan/pick
  6. `adapters/__init__.py:` 兜底 `return None` 替代 `create_wuhenai_adapter()`
- 裁缝老师纠错：#40 先猜后验、#41 OSS 架构误判、#42 热加载盲区
