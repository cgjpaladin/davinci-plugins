# 2026-06-12

## SMB + 公司机器同步清理

### SMB 同步
- 发现 SMB `shared/` 三个核心文件滞后（license.py、deploy_config.py、update_config.py）→ 已同步
- 发现 SMB 交付自检工具目录 `config.py` 版本号 2.2.1（应为 2.5.7）、`check_core.py` 滞后 → 已同步
- SMB `launcher.py` 缺 `-B` + `PYTHONUTF8=1` → 已补
- SMB 积压 `.pyc` 缓存（6/1 日期）→ 已清空 `__pycache__`
- 全量 diff 发现 8 个文件滞后（license/deploy/update/llm_providers/llm_typo/naming/updater + launcher_personal/shell_personal）→ 全部同步

### 个人版残留清理
- 全 21 台机器扫描 Edit/ 根目录
- 仅 mini140（张冀恒）有旧个人版壳文件 `交付自检工具.py` → 已删除
- mini140 还有个人版安装目录 `../交付自检工具/` → 已删除
- 其余 19 台（含 mini104/mini200 离线）无问题

### 运维发现
- 机器 SSH 别名参考 `~/WorkBuddy/达芬奇运维专家/config/machine_registry.json`
- 远程管理 skill 在 `~/WorkBuddy/达芬奇运维专家/.workbuddy/skills/远程管理/SKILL.md`
