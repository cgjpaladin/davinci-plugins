# 2026-05-24

## 批量命名工具 — 全面修复

### 幽灵文件（mock drop 双重注射）
根因：`if(!window.pywebview)` 在注入前判断为真，浏览器原生 drop 和 pywebview drop 同时触发。
修复：`setTimeout(() => { if(window.pywebview) return; ... }, 500)` 两处（app.js, app_table.js）

### 内容指纹去重
- `_seen_fp`（RenamerAPI 类变量）：size + 前 64KB MD5，跨批次
- `_sent_fps`（_on_drop 闭包）：防 pywebview evaluate_js 重放，过滤后计入 duplicates
- JS 侧：`fp||path` 去重 + `result.duplicates` 优先于本地重算

### 表版输入逻辑修复
- 双击检测：自制 350ms timer → `click.detail >= 2`（WKWebView 可靠）
- blur 提交：去掉 `_focused` 守卫（WKWebView `el.focus()` 不触发 focus 事件）
- rowClick：去掉 `_activeCancel()` 抢先还原
- 多选收缩：350ms 延迟 + 双击 clearTimeout
- method 批量写入：commit 中 onMethodChange 提前到写值之前

### 同源修复卡片版
- renderList 补 fp 去重安全网
- onDropResult 补 fp 去重
- mock drop 同修

### 桌面部署
- `批量命名工具-表格版.app`
- `批量命名工具-卡片版.app`

### 复盘发现
- "pywebview 注入前检查"是系统性反模式（3 处出现，2 处生产 bug）
- WKWebView 三大不可靠：focus 事件、DOM 全量重建、崩溃无日志
- 表版/卡片版 JS 分裂加速，共享模块缺失

---

## AI去字幕 — UX 深度重构

### 完成态 + 引擎错误 (18:50)
- 处理完成：绿条变绿 + 窗口标题「✅ 完成 - AI去字幕」
- 引擎挂了：PROJ_LB 标红「⚠ 鬼手剪辑 不可用，请切换到 无痕AI 2.1 重试」
- 下次扫描自动恢复

### 飞书 CLI 永久配置 (19:43)
- `lark-cli config init --app-id cli_a940d087f9b89cc9` 配好
- 读外部租户文档用 `--as bot`，配置文件 `~/.lark-cli/config.json`
- 无痕文档：`suiyu-network.feishu.cn/wiki/WUvXwI5vziT24qkAVdDcxTzLnef`
- 鬼手文档：`jollytoday.feishu.cn/docx/U73qdBhWbozFdpx4eTvcIO4gn7e`

### API 文档调研 (19:48)
- **无痕**：`GET /status` 返回 `data/progress` 0-100 整数（已在代码中使用，用 `weighted_progress/dur` 加权）
- **鬼手**：`POST /ve/work/status` 只有 `processStatus=-1/0/1/2`，无中间百分比
- 两者都支持 callback 回调但需要服务端 URL（未接）

### 估值公式重写 (20:12)
- SSH 扫 20 台机器本地 ops 日志，提取 66 条 `session_end`
- 无痕 46 条：单片段中位 132s，每+1≈+15s
- 鬼手 16 条：单片段中位 143s
- 公式：`150 + max(0, n-1) × 15` 秒 → `pricing_defaults.EST_BASE_SECS / EST_PER_CLIP_SECS`

### 进度条重构 (20:18-20:45)
**核心**：砍 `_update_countdown`，在 `SubtitlePipeline._get_progress_callback()` 重写整个进度系统。

- 绿条扩宽：`base(0.10) + ratio * scale(0.78)`，从窄带 0.40-0.60 → 0.10-0.80
- 阶段标签直写 ST_LB：`⬆上传中` / `📤提交中` / `🤖AI处理中` / `⬇下载中` / `🔧替换中`
- 动态倒计时：`elapsed/ratio×(1-ratio)`，3秒延迟 + 1%变化阈值 + <5秒显示"即将完成"
- 里程碑全解耦：`MILESTONE_*` 7 个常量（base 4 + 子类 3）
- `interface.py` + `set_phase()` 方法

**自检发现 2 个致命 bug**：
1. MILESTONE_ADAPTER_DONE 0.80 < adapter 最大进度 0.802 → 回退 → 改 0.81
2. `_st()` 假写——只设全局 `_phase_text` 不写 `itm[ST_LB].Text` → 阶段标签无声失效

**5 项解耦**：标签(PHASE_LABELS) / 映射(PROGRESS_BASE/SCALE) / 里程碑(MILESTONE_*) / 倒计时(ETA_*) / 估值(EST_*)

**死代码清除**：`_update_countdown` 全删 + `_t_start/_t_estimated/_task_count/_phase_text` 4 个死全局变量 + balance key 残余

**全量已推 SMB**。

### 可复用发现
- `BasePipeline.MILESTONE_*` + `set_phase()` → AI换口型/语音克隆等新工具直接继承
- `_get_progress_callback` 模式 → 任何 adapter 驱动类产品可照搬
- `pricing_defaults.EST_BASE_SECS/EST_PER_CLIP_SECS` → 估值常量集中管理
- 飞书 CLI `--as bot` → 任何需要读外部飞书文档的场景

---

## 批量命名工具 — 第二轮（18:18-21:37）

### 正则解析重构
- `_build_filename_re` 从 FIELD_CONFIG 自动生成，去掉手写硬编码正则
- 新增 `FALLBACK_RE`：缺字段旧文件名也能解析 Ep/Sc/Gr/Tk + v/status
- desc→method 推断加空值守卫：desc 为空时跳过，不猜"角色专属版"

### 去重系统根治
- `_seen_fp` 类变量→局部，消除跨批泄漏
- 删 `_sent_fps`、删防抖 hack——三重冗余去重全撤
- WKWebView 双火事件不再产生幽灵"全部重复"

### UX 补全
- 表头中文化（Ep 集数/Sc 场次/Gr 小场次）
- 行完成度色条（绿/黄/红）+ 空单元格暗红底 + 归档灰行
- desc 高亮闪现（0.35s 黄底 method→desc 联动可见）
- `result()` 持久结果条替代消失吐司
- 列宽/对齐/字号/颜色统一

### 输入校验
- `DIGIT_STRICT` 进 `commit`：Enter 提交也拦截 00
- 校验失败退回旧值不留死循环输入框

### 日志系统
- `_process_paths` 逐文件记录：文件名 + 解析结果 + 字段值
- `result()` 加 `call('debug_log')` 确认持久写入

### 构建部署
- `build_win.bat` + `app_icon.ico` 支持 Windows
- `build.sh` 改用系统 Python + ditto，去 `cocoa` 硬编码
- 卡片版移入 `card/` 文件夹备用
- 石家庄部署包 + 交接文档完成

### 代码清洁
- 删 23 个旧原型/构建产物/设计草图，37→13 核源文件

### SOUL 新铁律
- #37 不在日志里猜 bug
- #38 防抖是补丁不是根治
- #39 删文件前先确认是什么
