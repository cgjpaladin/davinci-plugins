# 2026-06-29

## 阿里云治理 — AK 降级 + OSS 日志 + FC 清理（21:00—22:40）
- 裁缝老师收到阿里云治理成熟度报告 376 项，逐条分析后 95% 是噪音（未购买 ECS/RDS/SLB 等服务）
- 核心操作：
  1. 主账号 AK `LTAI5t92...VBRU` → 禁用，改用 RAM 用户 `power-application-user` AK
  2. `.env` OSS_ACCESS_KEY 从主账号换为 RAM AK
  3. OSS 两桶 `wuhenai-clipflow` + `davinci-reports` 开日志
  4. FC 删除废弃函数 `license`（Python 旧版）、`license-v2`（弃用）
  5. 全链路验证：无痕 API 健康检查 + OSS PUT + 主账号 AK 禁用后 403 确认
- 裁缝老师纠正：先猜后验 → OSS 不再用主账号 ✅
- `.env` 备份在 SMB，可随时回滚

## Skill 创建
- `阿里云治理` skill：账号现状 + CLI 速查 + OSS 日志开启代码 + AK 替换流程
## 复盘 2026-06-29

**复盘产出**：
- FC 三 handler 补 trim+日志，PC 激活/心跳写入正常
- _get_stats() Windows 双平台采集（platform.win32_ver + winreg）
- 下载链路修复：ghproxy URL 纠正 + 3链路 + 逐条错误日志
- Base 字段改名 macOS版本→系统版本
- 安装脚本移除达芬奇运行检测
- ui.py sys.path 遮蔽×3 轮修复（插件shared优先 + 个人版跳过_smb_root + IS_PERSONAL NameError）
- dd-mbp 端到端验证通过
- 桌面 95MB 安装包 + 增量更新包重建
- 阿里云治理 skill + OSS AK 切换 + OSS 日志开启
