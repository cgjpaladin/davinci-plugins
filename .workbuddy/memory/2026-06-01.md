# 2026-06-01 工作日志

## 更新系统全面修复（v2.2.10 → v2.2.18）
- **30 次提交**，修复 21 个审计 bug + 端到端实测发现的 3 个连锁 bug
- **下载加速**：ghproxy 2s（vs GitHub Release 44s 从青岛）
- **安装修复**：`/tmp/_deli_src` 拷贝、TERM 环境变量、bash 语法、dicts 备份恢复、BOM 编码
- **UI 互斥**：`_BUSY` 全局锁 + `_lock_ui/_unlock_ui`，更新/检查/上传/AI校对互斥
- **发布加固**：zip 先于 version.json、GitHub Release、purge 状态检查、zip magic 校验
- **根因**：每轮没端到端测试就部署 → 修一个 bug 引入一个新 bug
- **铁律**：改更新系统后跑 `build → unzip → bash -n → 模拟安装` 四步再部署

## 报错上报全链路
- 上传 → 阿里云 FC → 查询下载 → TTTTTT 3 条日志成功送达

## 沉淀
- .feedback.md +2（#15 端到端测试、#16 子代理验证）
- MEMORY.md +端到端验证铁律、更新发布流程
- config.py __version__ = "2.2.18"

## 批量命名 5/28 复盘收尾
- `.feedback.md` +4（#24 method-desc联动、#25 _buildDesc自毁、#26 desc只读、#27 macOS分发）
- MEMORY.md +DMG分发节（xattr隔离清除、ARM64→Intel兼容、110MB体积说明）
- SOUL.md +铁律 #42（splice前必须rm -rf _build——缓存污染吞修复）
- 新建 `macos-dmg-dist` skill（user-level，跨项目通用）
- MEMORY.md 版本号 v2.5→v2.6

## 下午：配置页重做 + 激活码系统 + License 停用

- **配置页大改**：删轨道/阈值/系统词典，替换为 激活码 + DeepSeek Key + 飞书 App ID/Secret + 个人词典
- SMB 用户只显示个人词典，个人版显示全部 6 项
- API Key 密文显示（如 `sk-ce…08f6`），保存时检测掩码保留真值
- **一机一码 + 自助停用**：FC 加 `deactivate` action，插件配置页「转移授权」按钮
- 停用按钮试用期灰掉（检查 License is_trial）
- **pypdf 从增量包移除**：首次安装已有，更新不推。512KB→179KB
- **CSV 解压修复**：`zipfile.ZipFile(metadata_encoding="utf-8")` + 两 Launcher 加 `PYTHONUTF8=1`
- **词垫**：.env 中文注释 ASCII 编码崩（Python 默认编码）→ `encoding="utf-8"` 修复
- **批量替换事故**：3 次 `replace_all` 炸穿（函数名被替换、目录前缀丢失、构建器残留）
- **6 次发布版本号没 bump**：`sed s/旧/新/` 链式替换全部失效（config.py 一直卡 2.2.23）
- SOUL +铁律 #39（PYTHONUTF8=1）、#40（except:pass 必须写日志）、#41（replace_all 分步验证）
- `.feedback.md` +7 条（#17-#23）
- 版本：v2.2.18 → v2.2.31（本地+青岛）

## 晚间：更新系统深度修复

- 🪟 更新日志弹窗：v2.3.4→v2.3.7，10次迭代
- UIManager 踩坑固化：`GetItems()` 唯一正确模式，写 `davinci-ui-patterns` skill
- 根除 pyc：Launcher 加 `-B` 永不生成
- 更新链路：Releases API + ghproxy 优先 + importlib.reload 刷新
- 出厂检验 + bump_version.py 精确替换
- except:pass 全清 + 日志全覆盖
- 版本：v2.3.7 稳定版

## 晚间后期：支付入口 + 深度复盘

- 微信联系：配置页 + 主窗口显示「联系购买: 微信 paladinpp」
- 激活提示：配置页底部红字错误显示
- `tools/gen_key.py`：一键生成激活码
- 深度复盘：5 种系统性反模式 → SOUL +5（#42-#46），+.feedback.md 5 条（#24-#28）
