# 2026-06-16

## 交付自检工具 — 个人版安装/更新/公告全链路重构

### 构建与分发（build_personal.sh 重写）
- 文件夹名带版本号 `交付自检工具_v{VER}`，从 config.py 自动读取
- 产出格式：文件夹 → 单个 .zip（百度网盘下载文件夹丢失 +x 权限）
- 内层 zip 固定名 `请勿直接解压此文件.zip`
- Python.pkg 恢复官方原名 `python-3.13.13-macos11.pkg`
- xattr -c 清除 quarantine + 生成 先读我.txt（3 条 GUI 指引，零终端）
- 去掉多余外层 zip

### 安装脚本（install.command 全量重写）
- exec 3>&1 输出分流：终端仅 10 行，细节进 ~/Library/Logs/小裁缝工具/
- 欢迎页：留名 + 微信联系方式
- .command 改名 Mac安装.command
- 版本比较：unzip -p 管道读 zip → semver 比较 → 3 分支（相同/升级/降级）
- External Scripting 自动启用（sed 改 config.dat）
- 摘要→确认→密码 三步骤，不先斩后奏
- 密码取消静默退出（grep "User canceled"）
- 8 处 osascript \n 修复：单引号→$'...'

### 更新系统
- URL 顺序：raw version.json 提到第一（Releases API 无 history）
- 弹窗缩小 440×380，TextEdit Weight:1，正文加版本号
- GitHub Release 资产 _ → update_latest.zip（中文文件名乱码）

### 更新公告
- CHANGELOG.md 建立：markdown 编辑 → AI 同步到 version.json
- 8→6 条（删 v2.5.4/v2.5.5），全部用户语言重写
- 公告写作规范 5 条：emoji 开头/一行一条/说结果不说过程

### Agent 迁移
- 10 个 skill：.workbuddy/skills/ → .codebuddy/agents/（Skill 工具不扫描前者）

### UI
- 配置页 VGap：SMB 按钮 + 词典按钮各加 SPACE_SM / SPACE_TIGHT

### dd-mbp 测试准备
- dd-mbp 连接确认（ZT 10.163.15.58），改版本号为 2.0.0 待测更新全链路
- scp 中文路径用 base64 编码 Python 管道传输

### 复盘 Skill 改进
- Step 4「技能维护」→「文档与技能维护」
- 新增 4.1 MEMORY.md 去腐（逐条对照反模式表执行）
- 新增 4.2 knowledge/ 审计（已吸收/应迁/已过时/保留）
- 新增 4.3 Skill 全量巡检（不限于最近修改，全量扫重叠/废弃/过时引用）
- Phase 2 写入顺序加 knowledge/ 清理步骤
- 反模式速查区明确标注为 Step 4.1 的执行检查清单
- 达尔文双评委评估：A=81.9 / B=83.1 / 共识=82.5，安全铁律满分
- 按评估结果修复：打断恢复细化（读日报→搜标记→断点续/全新重启）、核心文件缺失 fallback（6 种文件/目录缺失的处理分支）
- 单检查点保留为设计意图

## 复盘 v3 — 技能维护补全（18:10）

上一轮复盘被裁缝老师说"还不够全面"，补充落地了 4 个承诺了但没执行的操作：

### 技能更新
| 技能 | 新增内容 |
|------|---------|
| davinci-ui | P5 bug #32（LineEdit+IME→SIGSEGV）、P4 输入安全规则 33-36、弹窗防连点模式、tkinter 子进程弹窗模式、osascript 换行语法 |
| davinci-publish | 阿里云 FC v3 部署、分发避坑表 5 项、安装脚本设计铁律 6 条、ttdd 测试机补充 |
| delivery-checker | 踩坑表 7 项（IME崩溃/中文穿透/osascript换行/防连点/URL不一致/GitHub中文名/网盘权限） |
| plugin-debugger | 崩溃日志分析流程、IME 崩溃特征签名 |

### 遗漏项回顾
- 上轮复盘提了"delivery-checker 需补 IME/input 踩坑"但没执行 → 本轮落盘
- davinci-ui 的 VGap/弹窗/tkinter 模式一直没写进 skill → 本轮全补
- license 排查的崩溃日志分析方法没系统化 → plugin-debugger 新增章节
- FC 部署命令散落在对话里没归档 → davinci-publish 新增章节

## 全项目文档审计 + 修复（18:15）

裁缝老师说"还不够全面" → 5 轮全量扫描，29 处发现，15 项已修复：

### 修复清单
| 文件 | 问题 | 修复 |
|------|------|------|
| MEMORY.md:24 | 交付自检 v2.5.5 | → v2.5.7 |
| MEMORY.md:6 | 头部日期陈旧 | → v3.1 2026-06-16 |
| MEMORY.md:345-351 | FC 章节仅 2 行 | 重写：完整表结构+环境变量+故障排查+踩坑 |
| MEMORY.md:110-124 | 缺少 IME崩溃/防连点规则 | +规则#17/#18，+UIManager表新增2行 |
| delivery-checker.md:10 | v2.0.29-dev | → v2.5.7 2026-06-16 |
| delivery-checker.md:721-744 | 激活码仍描述旧 LineEdit | 重写为 tkinter 弹窗模式 |
| plugin-skeleton.md:130 | deploy.py 不存在 | 替换为 build_personal.sh + push_all.sh |
| .workbuddy/TODO.md:87 | 错别字校对标记未实现 | → ✅ 已实现 |

### 新建文档
| 文件 | 内容 |
|------|------|
| `docs/cloud/license-system.md` | FC 端到端参考：架构图、路由表、环境变量、部署命令、故障排查、数据流 |

### 标记待用户确认
- CHANGELOG.md v2.5.7 notes 仅 1 行（用户需自行决定是否扩充）
- docs/plans/ 历史设计文档含旧 LineEdit 模式（💭 美观，不改）

### License/试用系统排查与修复
- 根因：_write_env.py 写入 FC 函数 license-yqvhkhvhgf（500），license.py 默认 license-node-mtqaghwijy（200）
- 新装机器全走了错 URL，试用记录未写入飞书表。客户端本地兜底正常
- 修复：_write_env.py、tools/gen_key.py URL 统一
- FC 部署：license_fc.js 更新到阿里云（FC v3 API `/2023-03-30/functions/license-node`）
- license.py _get_stats() 加达芬奇版本采集，飞书试用表加「达芬奇版本」字段
- 两台机器 .env URL 修复 + 凭证同步
- ttttt 激活码使用失败 → .env URL 修正 → verify_status 手写凭证修复

### UIManager IME 崩溃 → 弹窗重构
- 根因：Python 崩溃日志 `Fusion::RemoteApp::FindLocalObject + SIGSEGV`，达芬奇 Qt 层 IME 事件处理内存越界
- 激活码：3 LineEdit → tkinter 子进程独立窗口（Cocoa 渲染，不触发 UIManager）
- API key: LineEdit → Label(掩码) + osascript 弹窗编辑
- 6 个子进程弹窗按钮统一防连点（busy flag + Button.Enabled=False 灰显）
- tkinter 代码修复：_validate 定义顺序（函数必须在 Entry 之前）、中文 isalnum() 穿透 → isascii()、with hidden answer 缺空格 → f-string 拼接、withdraw/deiconify 消除闪现
- 复盘中承认"已知 bug"系编造 → 沉淀反幻觉教训

### 主窗口 LineEdit 清零（18:55—19:02）
- 子 agent 审计发现主窗口 `EDIT_SCRIPT_SRC` 和 `EDIT_SCRIPT_EP` 仍是 LineEdit
- 验证：ttdd 确实跑旧代码（我们只推了 dd-mbp，漏了 ttdd）
- ttdd 日志确认 7 次闪退（启动无关闭 = 异常终止），全部是配置页+主窗口 LineEdit + IME
- 修复：剧本链接→Label+🔗粘贴+📂浏览、集号→Label+编辑按钮
- 移除 TextChanged 事件（`dlg.On[EDIT_SCRIPT_SRC].TextChanged`），改为按钮各自调用 `_on_script_src_changed()`
- 新增 `_paste_link` / `_edit_ep` 处理器（osascript 弹窗 + 防连点）
- `_browse_script` / `_add_smb_path` / `_save` 补 `Enabled=False` 灰显
- `.env.example` 的 WB_LICENSE_URL 补修正（漏网之鱼）
- **最终结果：ui.py 零 LineEdit，IME 崩溃路径彻底消除**
- 两机同步：dd-mbp + ttdd 均为 0 LineEdit + 正确 URL
- git: 24 files, commit ad72889 → push main

### 下半场：死代码清理 + UI 重构（19:00-20:20）
- 发现问题：`_extract_characters`/`_split_episodes`/`match_timeline` 均为死代码
- 裁缝老师：「没懂，需要正则干嘛，剧本不是喂给 api 的吗」
- 方案：`parse_script` 只做格式提取，返回 `{"lines": [...]}`；AI 自行理解
- 删除：`_CHINESE_NUM`/`_cn_to_int`/`_parse_episode_number`/`_extract_characters`/`_split_episodes`/`match_timeline` → 共约 300 行
- 新增：`.txt`/`.md` 格式支持（`open().readlines()`）
- 集号输入整行删除（EDIT_SCRIPT_EP/LBL_SCRIPT_STATUS/btn_edit_ep/_edit_ep）
- LLM 提示词清理：删「--- 第N集 ---」标记、「用户指定集号」；修正序号 ①-④
- UI 布局重构：按钮改「📂 本地」「🔗 飞书」；提示文本置按钮上方
- 配置窗口防重入（`_config_open` 标志）
- 文件守卫：>20MB 拒绝 + 5 格式白名单
- VGap 补位：配置页授权区 3 处 + 字幕检测区 1 处
- `_browse_script`/`_add_smb_path`/`_save` 补 `Enabled=False`
- 飞书贴链接弹窗文案修正
- 翻车：正则批量编辑 script_parser.py → `_parse_episode_number` 夹缝残留 + 重复 import → 沉淀 SOUL #38
- 误判纠正：urllib（`_create_unverified_context` 是设计）/ Tree emoji（无崩溃记录）
- `.env.example` URL 修复 + `.feedback.md`/`SOUL.md`/`MEMORY.md`/`TODO.md` 更新

### 复盘
- 反馈 5 条追加（#10-#14）
- SOUL #38 新铁律：禁止正则批量编辑生产代码
- MEMORY.md 规则 #8 澄清 urllib 合法用法
- TODO.md 更新（去字幕交接 → 🔴，MD5锁/curl升级 → 🟡）
- git: 3 files 327 行未提交待 push

## 晚间复盘 v4（23:30—00:27）

### 交付自检 v2.5.7 下半场
- 四种安装方式确立（Mac手动/Win手动/Agent+GitHub/Agent+Zip）
- install_agent.py 设计：JSON行输出+need_sudo回调+--continue断点续装
- 死代码清理：_extract_characters/_split_episodes/match_timeline/_CHINESE_NUM/_cn_to_int/_parse_episode_number 全删（~300行）
- 按钮防连点：8按钮 Enabled=False+finally 统一
- 配置窗口防重入：_config_open 标志

### 文档体系
- README.md 替代 AGENT.md（人+agent双用）
- 先读我.txt 内嵌 agent 指引（# AGENT SECTION）
- license.py / README / 先读我 三处授权声明统一为公益+同情语气

### 纠错与修复
- 飞书用途修正：通知推送→文档读取（App ID+Secret 访问文档 API）
- 授权声明重写：禁止破解→公益初衷+欢迎读代码+只求别破解
- 全系统 6 处微信 paladinpp → 微信 paladinpp / B站 电影裁缝Bryan

### PC 交接（待验证，写入桌面 md）
- cross_platform.py / tk_dialogs.py / Win安装.bat / install.py
- 踩坑：bat CRLF / SSH+中文路径 / findstr ^转义 / macOS zip中文目录乱码
- 交接文档：pc219 桌面 交付自检_Win交接.md

### 持久化
- SOUL #39：跨 SSH 调试 Windows 脚本→交接本地 agent，不远程盲调
- MEMORY.md：v2.5.7 重构细节 + 安装体系 + 授权声明
- .feedback.md：创建，7 条新反馈（#14-#20）
- 桌面：交付自检_PC复盘待验证.md（待 pc219 agent 验证后补记）

### 遗留
- Win安装.bat 待 pc219 agent 重写并回传
- PC 验证清单待执行（10 项）
