## 2026-05-26

### 代码审查体系
- 建立 `CODE_REVIEW.md`（4 级 L0-L3 + 三产品专项清单）
- deep review AI去字幕 + 交付自检 → 交付自检中文 Type 兼容 bug 确认并修复（3 处）
- 交付自检死代码清理：`check_subtitle_typo` 函数 + `_SMB_PREFIX` 重复赋值

### 批量命名工具 v3.0 终结
- `renamer_web.py`：TK 扫-描去 fallback + 去 endswith + 注释修正 + 预扫描 200 上限 + 8 处 bare except→Exception
- `_splice.py`：默认表格版 + 版本注入改目标 `'3.0'`（git hash 恢复工作）
- `shared/naming.py`：FIELD_CONFIG `def:"01"`→`def:""`（_EMPTY_KEYS 拦截）
- `app_table.js` + `renamer_table.html`：版本号 v2.0→v3.0 两处同步
- `build_win.bat`：Python 版本检测 + ffmpeg 打包 + 图标缓存绕过 + 默认表格版

### Skill
- 两 skill 深重写：FIELD_CONFIG 真实代码、FIELD_RULES、TK 扫描、预扫描、_splice 嵌入、新字段 7 步 checklist
- pywebview 两 skill 合并 + Windows 图标缓存反模式
- Darwin 评估：批量命名 44.2→49.2，pywebview 46.7→47.7

### 石家庄
- 交接文档 12 章完成
- 全量部署包 + 两个更新包（skill 扩展 + bare except）
- 版本管理方案确定（OSS JSON → v3.1-v3.2）
- Windows 部署踩坑反馈并入代码+skill

### 其他
- 渲染队列工具全删
- MEMORY.md 版本号 v2.0.18→v2.0.30
