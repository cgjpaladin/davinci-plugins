## 2026-05-27 v3.2

- **多选编辑 bugfix**：commit 中用 `isMulti` 跳过 `finalVal!==oldVal`，防止发起行值不变时跳过其他选中行
- **序号动态补位**：`padStart(Math.max(2, files.length.length))`，≥100 文件自动 3 位
- **非视频提示**：Python `skipped` 单独计数，JS toast"格式不支持"而非"重复"
- **调试日志按钮**：右上角 `📋` 点击复制全部 debug_log，Python `_dbg_buf` 存最近 500 条
- **表头对齐修复**：`vertical-align:baseline` + `padding:6px 12px` 统一
- **全局 ellipsis**：`tbody td` 加 `overflow:hidden;text-overflow:ellipsis`
- **列宽调整**：镜头描述 120→149px，原文件名 auto→80px
- **desc 空值提示**：无 method 时显示"—请先选择制作方式"
- 版本号 v3.0→v3.1→v3.2，石家庄 3 轮更新包
- 达芬奇代码审阅 skill 加 S13（多选编辑规则）

## 创壹特供版 v1.0 — 首次交付

- 命名格式：`EP{ep}_SC{sc}_SH{shot}[-{shot2}-...]_TK{tk}_{desc}_{type}_{author}_V{ver}_{status}.{ext}`
- 9字段表格版，SH多镜内联 `[+][-]` 编辑器，type自动判定，desc仅AIPIC
- 完全独立目录，原版零改动；`naming_createone.py` 共享模块
- macOS .app 构建通过；Windows 打包 zip 已产出
- 关键修复：RGBA→RGB、is_dev用IS_PRODUCTION注入、_REQUIRED_KEYS去tk、SH禁止00
- 收工复盘：`.feedback.md`、MEMORY.md产品线、skill reference `inline-multi-editor.md`
