# 2026-06-17

## 凌晨紧急修复（00:27—02:21）

### SMB channel 泄漏修复
- 发现 SMB config.py `__channel__ = "dev"` → 全公司 20 台机器在 dev 模式
- 修复：手动 sed + 批量验证 mini101-105（104 离线）
- 根因：历史推送时 publish.sh 的 channel guard 还没加

### 三个 bug 修复

| bug | 文件 | 修复 |
|-----|------|------|
| 公司版左下角显示「试用权限异常」 | ui.py L2993 | IS_PERSONAL False 时 else 清空 ⏳ 占位文本 |
| 每次开配置自动弹 Finder | ui.py L1466-1482 | 6/16 commit 12f57119 缩进 bug，try 块误在 def 外面 |
| 更新提示 2.5.7→2.5.7 | ui.py L3002 | version_string() → __version__，不含 -dev 后缀 |

### Finder bug 排查过程
- 日志看不到问题 → 加了诊断日志（_edit_censor 入口 + Finder open 记录）
- 第一次诊断：traceback.format_stack()[-5] 越界 → 日志丢失
- 修正后：日志显示"即将打开 Finder"但"_edit_censor 触发"未出现
- git blame 定位：6/16 跨平台适配 commit 12f57119 → try 块缩进错了 4 格
- 全文件扫描无同类 bug

### sync.sh 强化
- 加 trap EXIT 自动切回 dev（与 push_all.sh 一致）
- MD5 锁正确拦截了手动修改 SMB → 手动重置 .push_commit 后同步成功

### 飞书使用手册
- 创建：T5D1dqybTo7IZ4xWvhfc9EZan2g（公开可读）
- OAuth 授权：docx:document:create + docs:permission.setting:write_only

### 垃圾清理
- auth_qr.png 清除
- 诊断日志代码清除

### 持久化
- SOUL #40：跨平台适配必须验证缩进
- .feedback.md：#21-#25 追加
- MEMORY.md：SMB channel 守卫 + dev 绝不碰 SMB
- consolidation-2026-06-17 tag

## 凌晨第三轮（02:45—03:16）

### 飞书使用手册按钮
- config.py：`MANUAL_URL` 常量 → `https://jcnjno6i0upk.feishu.cn/docx/T5D1dqybTo7IZ4xWvhfc9EZan2g`
- shared/_qr.py：纯 Python QR 码生成器（363 行，零依赖，byte mode + EC M）
- ui.py：`📖 使用手册` 按钮——放在「配置」下方（VGroup），和 BTN_START/BTN_CONFIG 同级
- 在线：`webbrowser.open(url)` 直接打开飞书文档
- 离线：tkinter 弹窗显示 QR 码 + 链接文字 + 复制/浏览器/视频教程按钮
- BTN_MANUAL 加入 lock/unlock（检查期间禁用）
- SMB 未同步（仅本地 dev 测试）
- git push

## PC 交接审查（03:46—03:50）
- 审查 pc219 桌面文档：交接/README.md 6.7KB（13步/15bug/8跨平台适配）
- other/ 文件夹：5 skill + fix_launcher.md + debug_round2.md（F1-F6 FATAL 待确认）
- mac_doc_convert_test.md：pc219 agent 留下的任务——验证 macOS .doc 备选方案
- 复盘 v6 落盘：.feedback #27-#31 + SOUL #41 #42

### PC 整合 + v2.5.8（02:26—02:45）
- pc219 agent 完成 Win安装.bat：GBK/CRLF/322行/13步完整流程
- 审查：`_PY=sys.executable` 非致命（launcher 自带 find_python 跳转）；`__file__` NameError 已加兜底
- bat 零在线下载验证通过（无 curl/python.org/http）
- 整合到仓库 + bump v2.5.8 + 重建 zip 95MB
- 桌面 `交付自检_PC复盘待验证.md` 更新：bat 部分已通过，插件调试待真实用户机
- 调试交接文档更新：启动链路修正（两次 Popen 跳转）+ 新增 4.6/4.7 已修 bug
