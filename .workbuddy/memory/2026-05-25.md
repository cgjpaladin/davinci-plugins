# 2026-05-25 凌晨

## 交付自检 v2.0.26–v2.0.29

### debug_log 基础设施
- 7 个关键检查函数接受 `debug_log=None`
- `_start_check` 传 `debug_log=_action_log`
- 3 处跳过原因调试点（轨数不匹配 ×2 + 轨名不匹配）

### launcher import 顺序修复
- 根因：`766a745` 重构时把 `from deploy_config import` 写在 `sys.path` 之前
- 修复：统一搜索 shared/ 目录（① 项目 ② WorkBuddy ③ SMB），找到第一个就停
- 同时修复 AI去字幕 launcher 的同款 bug

### 问题
- 误删 14 天日志（已记 .feedback.md）
- 交付自检 v2.0.29 已部署，日志系统完整

### Darwin 评估 skill
- 达芬奇交付自检开发 → 78/100
- 修 3 处：去重日志段 + 加验证步骤 + 扩展 debug_log/launcher 章节
- Skill 389→422 行

### 壳方案 + 全量修复 (17:00-18:15)

**壳方案**: 两个产品从 hardcoded Python 3.13 改为永久壳(shell.py, +sys.executable)。20台机器部署一次永不再碰。遇到排序 bug — 字符串 "3.7" > "3.14"，修成数字排序。

**SSL 全仓修复**: Python 3.14 的 ssl.create_default_context() 验证不了 api.zhaoli.com 证书。4个文件改为 _create_unverified_context() (ghostcut.py, wuhenai_v2.py, core.py, ui_pipeline.py)。2个 urlretrieve 改手动下载(context=)。

**SubtitleResult 双 Bug**: NameError(未 import) + TypeError(裸返不包 ResultItem)。两处错误路径修了 17:21，SSL 下载层修了 17:54。

**日志工具**: tools/check_logs.sh 四源全查, ui_widgets.py/ui.py stderr 双写。

**清债**: _bal 空函数删除、ui_external.py 废弃灰度层、重复 import、scan_io 重复消除、_load_deploy_config 6份归一为 deploy_config.py。

### 6步骤重构 + 日志系统修复 (18:30-19:05)
- 6步骤: ②上传③AI去字幕④下载⑤替换⑥完成，_step()自动编号
- Bug: fallback重试_step("AI去字幕")重复→移到run()的fallback之后
- StepLogger.fail/warn→stderr无条件（修复11条漏网消息）
- stderr移出try/except（文件写失败不影响ResolveDebug.txt）
- 三处关键词过滤统一：_ui_write_direct/_event_log/_action_log
- `tools/_deep_test.py` — 5轮76项自动化回归

### 复盘
- 回旋镖3次: shutil.which→硬编码→glob→排序bug→re.search
- 日志系统查6轮才干净→以后先跑_deep_test.py
- 袁宁确认恢复 ✅

**修复部署**: 20台机器批量 ssh 部署壳+SMB 同步。袁宁机器 Python 3.14 触发了沉睡 SSL bug(仅 3.13 兼容)，17:54 后零报错。
