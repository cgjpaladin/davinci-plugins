### 2026-05-12 反馈

#### 26. `GetItemListInTrack` 空轨返回 None
裁缝老师："打不开" / "nonetype object is not iterable"
→ 沉淀为：达芬奇 API 返回值不可信，`GetItemListInTrack` 空轨返 None 而非空列表，所有 API 遍历结果必须 `or []`

#### 27. check_color 缺 return 静默崩
裁缝老师："还是全部放行"
→ 沉淀为：删除代码后必跑全量验证 + 每个函数结尾 return 是硬检查点

#### 28. 跨文件 import 部署不可靠
裁缝老师："以后该怎么办？"
→ 沉淀为：launcher 自包含，不 import 外部模块（`_dev_hosts` 崩盘教训）

#### 29. 未经确认擅自 push_all
裁缝老师：（愤怒）"我没让你 publish，你怎么就给学员 publish 过去了啊？"
→ 根因：每次改完代码顺手跑 `push_all.sh` 且带 `GRAY_CHOICE=2`
→ 硬拦截：`publish.sh` 加入 `CONFIRM_PUSH=yes` 门，不在终端执行则拒绝
→ 规则：推 SMB 必须等裁缝老师明确说「发布」「push」「推全公司」才执行
→ 后果：同事用着 bug 版本

#### 30. 字幕样式 API 盲区（下版本待查）
→ 需求：读取字幕字体、字号、描边、阴影、颜色等格式参数
→ 现状：官方 README 的 TimelineItem.GetProperty() 仅返回 21 个变换键（Zoom/Crop/Rotation/Opacity），无任何文本格式属性
→ GetClipProperty 同样没有 Font/FontSize/Color/Stroke/Shadow
→ 下个达芬奇大版本（21+）Check API 是否新增
