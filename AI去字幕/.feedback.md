# 裁缝老师的纠错记录

> 重构阶段（2026-05-10）

1. BasePipeline 缺章节标题和展示细节——重构时删了太多 UI 层面内容
2. GhostCut print() 泄漏到日志区——需要统一 adapter 日志协议
3. WUHENAI_YUAN_PER_SEC 硬编码——缓存省钱按错费率
4. _cached_provider 初始值硬编码——切 adapter 时显示错供应商名
5. 余额汇率始终用 ACTIVE_PROVIDER——鬼手余额显示错
6. 灰度 gray.json 指向旧目录——灰度机器跑旧版本
7. 缩进错误——set_logger 吞掉 _access_token 等 init 代码
8. _log 回调签名不兼容——*args 修复单参 vs 双参
9. 下载进度/章节标题/空行等 UI 细节反复修正——应该在讨论阶段画图对齐
