# 代码审查标准

> 单人 + AI 协作的轻量审查体系。不是流程文档——是检查清单和判断框架。

---

## 一、审查分级

不是所有改动都需要同样深度的审查。按风险分级：

| 级别 | 触发条件 | 审查深度 | 最少耗时 |
|------|---------|---------|:--:|
| L0 自审 | 拼写/格式/日志/注释 | AI 自我核对 diff | 30s |
| L1 轻审 | 单文件改动，无 API/共享模块变更 | checklist + 冒烟测试 | 2m |
| L2 中审 | 多文件改动、新增功能、正则/解析器修改 | checklist + 边界测试 + 手动验证 | 10m |
| L3 全审 | 推全公司前、共享模块变更、破坏性改动 | checklist + 完整集成 + 灰度 + AI 对抗审查 | 1-2h |

判断规则：API 变更 > 文件数 > 行数。API 签名/共享模块变动自动升 L3。L3 不过不推全公司。

---

## 二、通用检查清单

### 正确性
- [ ] 改动是否解决了问题（不是盖住了症状）
- [ ] 是否有“时间和赛跑”的补丁（setTimeout/防抖当做修复）→ 找根因
- [ ] 边界条件：空输入、0、null、超长、特殊字符
- [ ] 回退路径：改坏了能不能 undo/回滚到上一个 commit

### 安全性
- [ ] 用户输入是否经过校验和清洗
- [ ] 文件路径是否防注入了 `..` 穿越
- [ ] API key / token 是否硬编码（必须在 `.env` 或 `deploy.json`）
- [ ] `.env` 和 `deploy.json` 是否在 `.gitignore` 中
- [ ] 第三方依赖是否有已知 CVE（`pip-audit` 或 equivalent）
- [ ] WebView：本地 HTML 是否加载了远程资源、CSP header 是否设置

### 数据流
- [ ] 跨批次状态是否泄漏（类变量、全局变量、闭包残留）
- [ ] 新增字段/参数与其他模块是否一致（Python → JS → CSS 三端对齐）
- [ ] 共享模块变更是否影响所有使用方

### 平台兼容
- [ ] Windows 路径分隔符、`--add-data` 分号
- [ ] macOS WKWebView 特殊行为（focus 事件、双火 drop、bottle HTTP 必须）
- [ ] 达芬奇 Fusion 内 `__file__` 不存在、API 返回值不可信
- [ ] Python 版本兼容（3.12+，不用 3.14+ 新语法）

### UI/UX
- [ ] 空状态有引导（不是空白页）
- [ ] 操作结果有反馈（持久结果条，不光靠吐司）
- [ ] 禁用态灰色可辨、鼠标悬停有反馈
- [ ] 非母语用户能否理解界面文字

### 测试
- [ ] 改完代码立刻跑 `test_smoke.py`（或该产品的冒烟测试）
- [ ] 新增功能有对应的测试覆盖
- [ ] 回归：改动相关的旧功能不受影响

---

## 三、达芬奇专项清单

### API 调用
- [ ] `GetItemListInTrack` 空轨返 None → 遍历前加 `or []`
- [ ] `GetClipProperty('Type')` 中文版返回「时间线」而非「Timeline」
- [ ] 测试时直连 GUI（不 nogui），达芬奇必须开着

### UIManager
- [ ] 控件 API：`ui.Widget({"ID": "id", ...})`
- [ ] 事件绑定：`win.On["ID"].Clicked`
- [ ] Tree 样式不可靠 → 交互勾选用其他方式
- [ ] 无 ScrollArea → 控件数控制在 ~20 以内
- [ ] VGroup 不裁剪溢出 → 验证窗口不撑破屏幕

### 部署
- [ ] launcher 用 `subprocess.Popen` 外挂 Python（Fusion 内 `__file__` 不存在）
- [ ] SMB 路径不写死 → 读 `deploy.json` 的 `smb_mount`
- [ ] 推全公司前灰度至少一台
- [ ] 部署后逐台扫描验证文件存在
- [ ] 灰度失败回滚路径明确：回退到上一版本 → 验证 → 重新灰度

---

## 四、批量命名工具专项清单

### 解析 & 命名
- [ ] `FIELD_CONFIG` 是单一来源——改字段顺序只改一处
- [ ] 新字段：确认 FILENAME_RE、FALLBACK_RE、build_filename、DIGIT_STRICT、_EMPTY_KEYS、HTML 表头/列宽 六处同步
- [ ] desc→method 推断有空值守卫（`desc` 为空不推断）
- [ ] `parse_filename` 两段解析路径均覆盖（主正则 + fallback）

### 前端
- [ ] `init()` 末尾有 `renderList()`
- [ ] DIGIT_STRICT 在 blur 和 commit 两处都有
- [ ] 校验失败退回旧值，不留死循环输入框
- [ ] `result()` 写 #resultMsg，不碰 statusText（被 `updButtons()` 覆盖）
- [ ] CSS 列宽/对齐/字号与 MEMORY.md 记录一致

### 去重
- [ ] `seen_fp` 是局部变量（不在类体或全局）——跨批次状态不泄漏
- [ ] WKWebView 双火不产生幽灵重复

### 构建
- [ ] macOS `build.sh` 用系统 Python + ditto
- [ ] Windows `build_win.bat` 用 `edgechromium` + 分号 `--add-data`
- [ ] 两版构建通过：`build.sh table` + `build.sh`
- [ ] 冒烟测试 22/22

---

## 五、交付自检专项清单

### 检查项
- [ ] 新增检查项：注册表 + 门机制 + 分级（warn/fail）三处同步
- [ ] warn 仅 5 类：夹帧/直通/异体字/系统词典/尾板
- [ ] 门机制：并行四扇，严格/宽松两种模式
- [ ] 路径检测无 gate 永远先跑

### AI
- [ ] 错别字：DeepSeek→千问 自动降级
- [ ] SHA256 缓存不过期

---

## 六、审查执行方式

### AI 自审（每次 commit 前）

```
1. git diff --stat 确认改动范围
2. 逐文件扫 diff，按清单逐项核对
3. 发现不确定的问题 → 标注 🟡 或 💭 → 问裁缝老师
4. 跑冒烟测试
5. commit message 注明审查级别：`[L1]` / `[L2]` / `[L3]`
```

### 裁缝老师终审（推全公司前）

```
L3 审查必须裁缝老师亲自过目——AI 审查是补充，不是替代。
- 在达芬奇里完整跑一遍
- 确认 UI 看起来对（不是在 AI 脑补里对）
- 灰度一台，确认无问题再全量
```

### AI 对抗审查（L3 附加）

```
单人开发无第二审阅者 → spawn code-review-expert agent，额外做：
1. 扮演恶意用户：输入 00、空字符串、超长字符串、特殊字符 → 边界崩了吗
2. 扮演攻击者：路径穿越、XSS 注入、token 泄漏 → 安全面崩了吗
3. 验证回滚路径：git revert → 构建 → 冒烟测试 → 能回得去吗
```


### 审查记录

审查中发现的问题写入 `审查记录.md`（按日期追加），格式：

```
### YYYY-MM-DD | 产品名 | 审查级别
- 🔴 desc→method 空值未守卫 → 已修复
- 🟡 列宽与设计规范不一致 → 待确认
- 💭 考虑提取共享 validation 模块
```

---

## 七、代码质量标准速查

| 维度 | 🟢 好 | 🟡 可接受 | 🔴 不可接受 |
|------|-------|----------|-----------|
| 去重 | 局部变量 + 指纹 | 类变量但明确清理了 | 防抖 + setTimeout |
| 校验 | 类型+范围+格式三重 | 仅范围检查 | 无校验 |
| 错误处理 | 精确消息 + 恢复路径 | 静默 continue | 裸 except + pass |
| 日志 | 逐条目可追溯 | 汇总但有关键信息 | 无日志 |
| 命名 | 一看就懂 | 需要对照文档 | 名不副实 |
| 状态管理 | 无跨批泄漏 | 有泄漏但已文档化 | 无意识泄漏 |
| 测试 | 覆盖主路径+边界 | 仅主路径 | 无测试 |
| 文档 | 代码即文档 + 设计决策 | 有注释但缺设计决策 | 看不懂的设计 |

---

*Created 2026-05-25 | Code Review Expert*
