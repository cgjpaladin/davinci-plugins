# 批量命名工具 · 卡片版 — UX 架构重构

> ArchitectUX | 2026-05-24 | 基于卡片版专属设计意图

---

## 0. 先对齐：这个工具是给谁的

**不是新用户**。是一个剪辑总监 + 20 个剪辑师，每天处理几十上百个素材。
**不需要教育**。Ep/Sc/Gr/method 所有人都懂。
**核心目标**：从 Finder 拖文件 → 快速填元数据 → 一键归档到项目目录。速度第一，零思考，闭眼操作。

---

## 1. 为什么留卡片版

卡片版的本质优势——**缩略图 + 专注编辑**：

```
┌────────────────┐
│ 🎬 缩略图       │  ← 剪辑师通过画面识别镜头，不是通过文件名
│ Ep01_Sc02_Gr03 │
│ 智能分镜_双轨版  │  ← 一行文件名预览，所见即所得
│ ← 原名.mov     │
└────────────────┘
```

表格版 8 列并排要求所有字段同质化——Ep/Sc 一样、method 集中在 2-3 种。但剪辑师的素材差异大：同一批可能有 3 种 method、5 个作者、各不相同的 desc。列对齐在这种情况下是噪音——每行各自的字段填得都不一样，并排对比没有意义。卡片版一个一个来反而更舒服。

---

## 2. 当前断点

### 2.1 情绪曲线

拖入 → 看到卡片 → 全灰 → 点第一张 → Inspector 八个字段全空 → "从哪开始" → 填一个点一下下一张 → 填完 → 归档 → toast 消失 → 不知道文件在哪

### 2.2 具体痛点

| # | 痛点 | 根因 |
|---|------|------|
| 1 | Ep/Sc 逐张填 | Inspector 只作用当前选中的一张卡片，没有批量设 |
| 2 | 不知道哪些卡填完了 | 卡片没有完成度视觉标记 |
| 3 | 不知道 Inspect 里哪些字段还没填 | Inspector 所有字段外观一致，空值不突出 |
| 4 | method 改了 desc 跟着变，但用户没注意到 | 联动不可见 |
| 5 | 操作结果一闪而过 | toast 2.5 秒消失 |
| 6 | 卡片多时难以定位未完成的 | 无筛选/排序，只能滚屏扫 |

---

## 3. 六层重构架构

保留 Inspector 模型，不推翻。加层、补漏、不重来。

```
┌──────────────────────────────────────────────────────────────────┐
│  ① 顶部操作栏  [路径] [归档] [重命名] [撤销]                     │
├──────────┬───────────────────────────────────────────────────────┤
│          │  ② 批设栏  Ep [01] Sc [01] 制作 [▾] [应用到全部]      │
│ Inspector│  ③ 状态栏  ✅ 5  ⚠️ 3 待填  ❌ 2 空                    │
│          │───────────────────────────────────────────────────────│
│  [选中]  │  ④ 卡片列表                                           │
│          │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  当前卡  │  │🎬  ✅│ │🎬  ⚠️ │ │🎬  ⚠️ │ │🎬  ❌│ │🎬  ✅│      │
│  字段值  │  │Ep01  │ │Ep01  │ │Ep01  │ │Ep—  │ │Ep02  │       │
│          │  │OK    │ │—     │ │—     │ │—    │ │OK    │       │
│          │  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘       │
├──────────┴──────────────────────────────────────────────────────┤
│  ⑤ 通知面板  上次归档: ✅ 7 · ⚠️ 3 · /EP01/SC02/...     [×]    │
└──────────────────────────────────────────────────────────────────┘
```

### 层① 操作栏 — 不变

### 层② 批设栏 (新增)

**何时出现**：文件拖入后，Ep/Sc 列为空时自动展开。填满自动收起。可手动收起。

```
Ep [01]  Sc [01]  制作 [智能分镜版 ▾]  作者 [        ]  v [01]  通过 [OK ▾]
[ 应用到所有空行 ]    [ 应用到所有空行 ]   [ 应用 ]  [ 应用 ]
```

每个字段一行，已填过的行不覆盖。点"应用到所有空行"后该字段后面所有卡片更新，批设栏那一行变绿表示已全满。

### 层③ 状态栏 (新增)

固定一行，三个数字：

```
✅ 5 已就绪    ⚠️ 3 缺字段    ❌ 2 完全未填    [仅显示未完成 ▸]
```

数字可点击 → 过滤卡片列表。"仅显示未完成"折叠已就绪的卡片。

### 层④ 卡片增强 (部分已实现)

每张卡新增：
- **完成度色条** — 左侧 3px (绿=满 / 黄=缺 / 红=空) — 今日表格版同款
- **空缺字段摘要** — 卡片底部小字灰色 "缺少: 制作方式, 制作者"
- **归档灰化** — 今日已实现

### 层⑤ Inspector 增强

**当前**：所有字段平铺，无优先级，无空值提示。

**改进**：

```
┌─ 必填 ──────────────┐
│ Ep   [01]   ✅      │
│ Sc   [02]   ✅      │
│ Gr   [03]   ✅      │
│ 制作 [双轨版 ▾] ✅   │
├─ 自动 ──────────────┤
│ 描述 智能分镜   🔒   │  ← 由制作决定，浅灰只读
├─ 补充 ──────────────┤
│ 作者 [_____]   ⚠️   │  ← 空值红边框
│ v    [01]     ✅    │
│ 通过 [▾ OK]   ✅    │
└─────────────────────┘
```

关键改动：
1. **分组**——必填 / 自动 / 补充，视觉分区
2. **空值标红**——没填的字段红色边框 + "⚠️"
3. **联动可见**——method 下拉旁一个小箭头 → desc 字段高亮闪烁一秒
4. **TK 不显示**——TK 是自动计算的，放 Inspector 里混淆视听。显示在卡片文件名预览里就够了

### 层⑥ 通知面板 (新增)

替代 toast。固定底部，可折叠。每条结果保留到用户关闭。

---

## 4. 实施优先级

| P | 项目 | 价值 |
|----|------|------|
| P0 | 层② 批设栏 | 消灭 #1 痛点——"Ep/Sc 逐张填" |
| P0 | 层③ 状态栏 + 筛选 | 消灭 #2、#6——"不知道哪些还没填完" |
| P1 | 层④ 卡片完成度色条 | 一眼扫出缺字段的卡片 |
| P1 | 层⑤ Inspector 分组 + 空值标红 | 消灭 #3——"不知道当前卡缺什么" |
| P2 | 层⑤ method→desc 联动闪烁 | 消灭 #4 |
| P2 | 层⑤ 去掉 TK 字段 | 降低 Inspector 噪音 |
| P3 | 层⑥ 通知面板 | 消灭 #5——toast 消失 |

---

## 5. 架构约束

- **WKWebView**：只做增量 DOM 更新，不 `innerHTML` 全量重建
- **16-18 卡片上限**：不做虚拟滚动
- **暗色主题**：所有新增组件跟随 `var(--tbl-bg)` 等现有变量
- **Inspector 保持动态生成**：`_buildInspector(FIELD_CONFIG)` 不改底层机制
- **卡片版专属**：不向表格版妥协

---

*ArchitectUX | 2026-05-24*
