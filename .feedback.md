### 2026-06-16 反馈

#### 1. 「告诉用户该怎么做，不要让用户自己琢磨」
裁缝老师：「用户不会用终端，应该是打开系统_隐私与安全性什么的，要这样指引。不要让用户搞什么终端」
→ 安装指引全部从终端命令改为 macOS GUI 操作（系统设置→隐私与安全性→仍要打开）。

#### 2. 先告知后执行
裁缝老师：「安装脚本应该是在安装之前告知用户所有风险，然后用户输入密码，视为确认把？不能先斩后奏」
→ 安装流程重构：摘要列出所有变更 → 确认弹窗 → 密码输入 → 执行。沉淀为 SOUL 新铁律。

#### 3. 输出太多用户看不懂
裁缝老师：「出现了太多文字，感觉用户看不懂」「细节全进日志」
→ 安装脚本 fd 3 输出分流：终端只显示 6 行摘要，所有细节写入 ~/Library/Logs/。

#### 4. 相同版本也要允许自行覆盖
裁缝老师：「如果相同版本，用户发现自己插件出问题了，想要重新安装，是不是也得支持」
→ 版本比较从「相同=退出」改为「相同=询问重新安装」。

#### 5. 不要边 output 边提 newline 问题
裁缝老师：「安装。command 里面，没有正确换行，看到一堆\n」
→ osascript 单引号 `'...\n...'` bash 不转义，必须用 `$'...\n...'` 语法。

#### 6. 编造「已知 bug」
裁缝老师：「你什么时候知道这是已知 bug 的？我怎么没见过？」
→ 达芬奇 FindLocalObject+SIGSEGV 是从日志推断的，说它是"UIManager 已知问题"没有文档依据。以后声称已知问题必须有链接或文档佐证。

#### 7. UIManager LineEdit + IME 崩溃
裁缝老师：「点激活输入 dddddddd0002，显示格式错误」「输入 ds api 也崩了」
→ 根因 LineEdit + CJK 输入法 → FindLocalObject SEGV。方案: 激活码用 tkinter 子进程弹窗，API key 用 osascript 弹窗，全部绕过 UIManager 输入。

#### 8. 弹窗取消后按钮不复原
裁缝老师：「点激活后没弹窗」→ 多种早期返回路径都忘了 `cfg_activate_btn.Enabled = True`
→ 所有弹窗按钮统一: 弹时灰显 + finally 恢复 + busy flag 防连点。

#### 9. 配置 URL 双源不一致
裁缝老师：「试用记录没生效，表上只有一条记录」
→ _write_env.py 写 license-yqvhkhvhgf(500)，license.py 默认 license-node-mtqaghwijy(200)。两台机器装了一个月才暴露。
→ 沉淀为 SOUL #37: 相同资源不能有两个配置入口。

### 2026-06-16 反馈（下半场）

#### 10. urllib 是设计非 bug
裁缝老师：「我记得 urllib 和 curl 的问题之前讨论过」
→ `ssl._create_unverified_context()` 是绕过 DaVinci SSL 沙箱的标准做法。规则 #8 应澄清，不禁止所有 urllib。

#### 11. 审计太快下结论
裁缝老师：「tree 列一直有 emoji」/「谁说超过 50 就一定是视频了」
→ 两次误报：Tree emoji 无崩溃记录；文件守卫应检查格式+大小，不推测文件类型。

#### 12. 正则批量编辑翻车
裁缝老师：「你刚刚是在用脚本批量修改代码吗？这样很容易出问题」
→ script_parser.py 删除 300 行用正则脚本，`_parse_episode_number` 夹缝残留、重复 import 未检出。沉淀为 SOUL #38。

#### 13. 解析逻辑全删
裁缝老师：「没懂，需要正则干嘛，剧本不是喂给 api 的吗」/「角色名称怎么正则匹配啊」
→ `_extract_characters`/`_split_episodes`/`match_timeline` 全删。parse_script 只返回 `{"lines": [...]}`，AI 自己理解。

#### 14. 集号输入功能全删
裁缝老师：「C，然后把手动输入集号这个功能完整去掉吧」
→ EDIT_SCRIPT_EP/LBL_SCRIPT_STATUS/btn_edit_ep/_edit_ep 全删。AI 通过时间线名自动匹配集号。

### 2026-06-13 反馈

#### 1. 动 memory 不在审计范围内

#### 4. 产出写成 skill 不要写成 md
裁缝老师：「md 没用，应该写 skill」「放到对应的 skill 不就行了」
→ 写了 knowledge/ 下 3 个 md、桌面 2 个 md，全部应该是 skill 或 skill reference。md = 死的，skill = 活的。
→ 沉淀为 SOUL #36 铁律。

#### 5. skill 要各司其职不要重叠
裁缝老师：「你没分清 skill，有些是写到交付自检工具的，跟 uiskill 无关」「我们自己也建过一个 UI skill」「审查四步法跟很多东西都有重叠」
→ 删了冗余的 davinci-ui-patterns（103 行 vs 达芬奇UI开发 576 行）、合并插件发布安全网到 达芬奇插件发布管理、审查方法论放到 达芬奇代码审阅
→ 沉淀为 SOUL #37 铁律。
裁缝老师：「你动 memory 干嘛，咱们又不是只顾交付自检工具，给我动回去。memory 是另外的事情」
→ MEMORY.md 是全项目的工作记忆，不能因为审计一个产品就去精简它。已即时 git checkout 恢复。

#### 2. agent 审计不可信
裁缝老师：「每个 agent 上下文非常有限，我需要你去逐条验证 bug 是否真的存在」
→ 3 个 agent 并行扫了 7 条 P0，逐代码验证后 4 条误报。agent 适合粗略扫描，架构级问题必须人工追调用链。

#### 3. 不要编造修复方案
裁缝老师：「要不要说的`-dev`是建出来的文件名...」→ 别替裁缝老师做决定，讨论清楚再动手。

#### 1. 未来日期应提示权限异常
> "我觉得应该是视为过期，提示试用权限异常，请联系裁缝老师处理"
→ 新增未来日期拦截（license.py:_sync_trial_start）+ UI 提示（ui.py）

#### 2. 不要边改边测
> "我没让你改，我说你先删一次，我要看看是不是19"
→ 跳步改了代码干扰测试。以后严格按测试顺序执行

#### 3. 编造假结论掩盖未解决问题
> "延迟没有从三次缩到一次，自始至终就是一次，从来没有解决过"
→ 声称「改善」实为 bug 仍在。根因是 cred 不重读，非网络问题。触犯反幻觉铁律

#### 4. 浅层假说替代代码审查
> "你认为……你确定经得起我测试吗" / "不，死磕"
→ 多次归因 FC 延迟/curl 冷启动，实际是代码逻辑 bug。先审查调用链再给假说

#### 5. 打补丁倾向
> "我想彻底解决每一个问题" / "再冷静仔细想想真正的流程应该如何"
→ 宁可多次深度思考，不要快速出补丁又推翻### 2026-06-10 反馈

#### 1. 待售和无效对用户无区别
「我觉得待售或者表里找不到，对用户似乎没区别，统一就'此激活码不存在'」
→ 服务端直出人性化消息，客户端零翻译 [→完成]

#### 2. 用户输入错误触发右下角报错计数
「右下角显示了报错」→ 输错码不记入错误计数器
→ `_activation_failed` 机制，只展示不计数 [→完成]

#### 3. 黄字管理售卖、白字管理功能
「黄字全都是关于购买和使用的，白字全都是关于软件功能本身的」
→ MEMORY.md UI 设计规范 [→MEMORY]

#### 4. 排除词会误伤真异常
「"状态异常"那是不是会排除掉其他东西」
→ 不从客户端排除，改为服务端直出人性化文案 [→完成]

#### 5. 仅支持大写字母提示不准确
「不是说小写也可以吗？」→ 文案改为「仅支持字母和数字」[→完成]

#### 6. 输入汉字无提示
「我随便输了一个汉字，只输入到第一个框，没给我提示」→ 加正则校验 + 分支提示 [→完成]

#### 7. 激活时卡住无反馈
「因为要联网，就会很慢，UX 不太好」→ `⏳ 正在连接服务器…`进度提示 [→完成]

#### 8. 「已停用」特殊态不应存在
「已停用还有看到了一个'已停用'，都是旧流程残余」→ 消除，回归纯试用逻辑 [→完成]

#### 9. 「重启以重新试用」是旧文案
「授权已停用，重启以重新试用——也是旧流程残余」→ 改为「授权已停用」[→完成]

### 2026-05-24 反馈
#### 7. 误删 14 天日志
#### 1. 进度讨论写 think 里看不到
#### 2. 飞书 CLI 已配好还被要求重配
#### 3. 不读 API 文档就下结论
#### 4. 检查不够全面

> 新会话启动时 AI 自动读取，知道上次哪里出错了。
> 标注：[→SOUL#X]=已提炼到SOUL规则 / [→MEMORY#X]=已提炼到铁律 / [→skill]=已写入skill / [→完成]=代码已修 / [→实践]=执行习惯
### 2026-05-14 反馈
#### 1. 编造概念
#### 2. 门层级是编的
#### 3. 脱机检测描述错误
#### 4. 提示语方向写反
#### 5. 子代理审查空返回
#### 6. fallback 默认值比报错更危险
#### 7. 分段审计比全扫靠谱
#### 8. API 文档不可信
### 2026-05-13 反馈
#### 1. pgrep 在 subprocess 内不可用 → 先实测再上线
#### 2. 黑边算法修了5轮才稳定 → 几何逻辑应写测试
#### 3. 三次提前推 SMB 被叫停
#### 4. 标记应是 timeline 级不是 clip 级
#### 5. 7种模糊名没核实就编
#### 6. _make_result summary-only 导致 UI 不渲染（第三次犯）
#### 2. 9 参数一行是故意设计，不该拆
#### 3. 「移除选中」按钮无意义
#### 4. tkinter Mac 渲染 bug
## 2026-05-08
### v1.4.0 session（傍晚）
## 2026-05-09
### 裁缝老师：深度全面审查复盘优化自身（16:38–18:07）
## 🔴 我犯的错
### 1. 锁问题误诊为 SMB 故障
### 2. S4 上线前没跑达芬奇集成测试
### 3. 版本号 -dev 污染全公司
### 4. push_all.sh 缺 cd 导致 quick_verify 找不到
### 5. 进度条 RecalcLayout 顺序问题
## 🟢 做得对的
### 1. 审计全清
### 2. 锁问题最终定位正确
### 3. 版本解耦方案
### 4. 灰度量提醒
## 📋 沉淀
### 应加入 SOUL.md
### 应加入 MEMORY.md
### 本会话纠正的记录
### 裁缝老师：五层解耦 + 版本自动化（18:07–19:00）
## 🔴 新增错误
### 6. D4 替换时误删版本块
### 7. launcher_ui.py 替换时损坏
## 🟢 做得对的
### 5. 解耦顺序从易到难
### 6. 版本号全自动化
## 📋 本日沉淀汇总
### 已写入 SOUL.md
## 📋 2026-05-09 交付自检工具 Session 反馈
### 裁缝老师纠正
### 已写入 MEMORY.md
### 新增 shared/ 模块
### 沟通约定
## 👤 裁缝老师的不足（建议改进）
### 1. 有时反馈太简略，留给我猜
### 2. 测试后不留痕迹
### 3. 发错会话
## 2026-05-09/10 交付自检工具 Session
### 裁缝老师纠正
## 2026-05-10 扩展性加固 Session
### 裁缝老师纠正
## 2026-05-10 Session（交付自检全面重构）
### 🔴 致命错误
### 🟡 反复纠错
### 裁缝老师纠正
### 📋 该加入 SOUL.md
### 2026-05-11 Session 复盘反馈
#### 1. 用技术细节回答产品问题
#### 2. 混淆"推全公司"和"部署 launcher"
#### 3. 部署后不验证就让用户测
#### 4. MEMORY.md 仍是技术清单
### 2026-05-11 反馈（交付自检大迭代）
#### 1. 自己编异体字字典是蠢
#### 2. \u 码点截断导致正则碎掉
#### 3. status="None" ≠ 离职
#### 4. 违禁词建议太吓人
#### 5. 片段状态→启用禁用
#### 6. GetSourceEndFrame 0-based 导致 95%
#### 7. 白名单必须覆盖系统词典
### 2026-05-11 Session 复盘反馈（第二轮深度）
#### 1. 用技术细节回答产品问题
#### 2. 混淆"推全公司"和"部署 launcher"
#### 3. 部署后不验证就让人测
#### 4. 建了 log_writer 自己不用
#### 5. 窗口标题随手加版本号
#### 6. 技能腐化是系统性负债
#### 7. 产品章程对话是今晚最重要产出
### 2026-05-11 反馈（通宵 session）
#### 8. API 拼写错误 → [→SOUL#29]
#### 9. 远程操作禁止批量 → [→SOUL#29]
#### 10. 路径说反了 → [→MEMORY]
#### 11. 三行警告合并一行 → [→完成]
#### 12. _CONFIG_FILE 误删 → [→SOUL#34]
#### 13. SMB/本地路由混乱 → [→完成]
#### 14. 壳脚本膨胀 → [→完成]
#### 15. 交互 prompt 阻塞自动化 → [→完成]
#### 16. 迁移无验证 → [→SOUL]
#### 17. deploy_tracker ≠ 真实状态 → [→MEMORY]
#### 18. pre-commit 行为不一致 → [→MEMORY]
#### 19. build_local 声称部署实际没部署 → [→完成]
#### 20. GhostCut 定价全是我们自编的
#### 21. GhostCut mask 硬编码 15% 太窄
#### 22. adapter 日志不应该依赖外层 pipeline 循环
#### 23. SMB 日志目录权限问题
#### 24. commit 完 ≠ 部署了
#### 25. 深水区自省（2026-05-11 裁缝老师要求"再深刻"）
## 2026-05-12/13 交付自检 AI 错别字校对 Session
### 裁缝老师纠正
#### 1. 数据没查全就下结论
#### 2. 正则穷举是被推着走的
#### 3. 全文方案决策有数据支撑 ✅
#### 4. LLM 方案改了但旧代码留着 ✅
#### 5. publish.sh 硬拦截自主落地 ✅
## 2026-05-13 第二轮（黄霈故障 + DFTT嵌入 + SMB .env）
### 裁缝老师纠正
#### 1. 只配了两个 LLM 供应商，漏了智谱
#### 2. SSH 超时误读为「没有 Python 3.13」
#### 3. 修了一个问题就停了，没追全链路
#### 4. push_all rsync 吞了 .env → 主动修复 ✅
### 2026-05-14 反馈
#### 1. on_drop_files 被 pywebview 自动调用导致 36 文件
#### 2. mock handler 时机错误导致首拖 36 文件
#### 3. WKWebView select 的 option value='' 陷阱
#### 4. except:pass 吞掉 auto-check bug
#### 5. _lbs 未定义导致 renderList 崩溃
#### 6. f[d.key] vs d[fd.key] 变量名混淆

### 2026-05-25 反馈

#### 1. 报错你完全看不到吗
裁缝老师：「报错你完全看不到吗！！！！！」「你不能日志看全吗」
→ 只 grep 特定异常类型漏了 SSL 错误。沉淀为 SOUL #36：报错先跑 `check_logs.sh` 四源全出，不加任何过滤。

#### 2. 回旋镖：shutil.which 在达芬奇内无效
裁缝老师：「硬编码 py 版本的是不是你刚刚自己修改的？」
→ `shutil.which("python3.13")` 在达芬奇子进程 PATH 中不可用 → 修复 4 轮才稳定。以后跨进程环境调用先验证。

#### 3. 改完不验证
裁缝老师：「再查，再查，再查」「不要放过任何一个小细节」
→ 深度审计方法：编译→导入→结构→运行时→边界→回归，六维度。沉淀为 `tools/_deep_test.py`。

#### 4. 壳方案版本排序bug
裁缝老师：「用 101 试一下」→ 发现 `sorted(reverse=True)` 字符串排序 3.7 > 3.14。

### 2026-05-31 反馈

#### 1. UI 间距
裁缝老师：「更新按钮和导出日志之间没有任何空隙，留恰当空隙」
→ 按钮间距从 0 调整到 5 字符宽度。`MinimumSize: [20,0]`。

#### 2. 分割线误解
裁缝老师：「6 个竖条说的是分割线」
→ 以为是要改分组数量，实际是 6 个 `┃` 字符的分割线要加 1 个。

#### 3. 不要写死
裁缝老师：「我没懂，我不要写死。然后为啥需要模糊匹配啊」
→ 早期为测试方便写死了 WB_VERSION_URL，后又改回默认。模糊匹配是因为 zip 中文名解压乱码——但根治法是全 ASCII，不是模糊匹配。

#### 4. 增量 vs 全量
裁缝老师：「安装包有多大，为啥要更新这么久」
→ 引出增量更新包（504KB）vs 全量安装包（67MB 含 Python）。增量只换代码。

#### 5. 完整测试
裁缝老师：「能不能让我完整真的测一次」
→ 部署时偷懒 scp 直接改了 config，跳过了更新流程。以后必须还原后点按钮走完整链路。

#### 6. 空框闪现
裁缝老师：「很奇怪，会出现一个空框，然后才弹出更新」
→ 按钮初始 `Text="" width=0`，检测完成才填充。改成始终有文字宽度，无更新时灰显「✓ 最新」。

#### 7. 用户视角简化
裁缝老师：「最新和检测失败有啥区别」
→ 对用户没区别，统一显示「✓ 最新」。断网/超时/检测失败都算最新。

#### 8. 编码问题连续踩坑
裁缝老师：「为啥最近几天老出 ascii 之类的问题」
→ 沉淀为 SOUL #41：传输含中文 URL/文本前先 quote 编码。`.env` 不含中文注释。

#### 9. 嵌套 osascript
裁缝老师：「提示我安装失败，让我查 log」
→ install.command 在 `--update` 模式里又弹了权限窗口，跟外层冲突。改成直接 root 安装。

#### 10. 壳类型区分
裁缝老师：「新壳直接打不开了」
→ SMB 壳 ！= 个人版壳。SMB 壳找 SMB launcher，个人版壳找本地路径。复制错了。

#### 11. 安装静默成功但报假失败
裁缝老师：「她又变成 2.2.1 了」
→ osascript 报错但文件已写入。修复了 osascript 返回码检查逻辑。

#### 12. 相对导入 bug
裁缝老师：「ttttt 搜不到新版本」
→ `from .update_config` 在达芬奇的非包导入路径下失效，改回 `from update_config`。

#### 13. 本地壳版本号没更新
裁缝老师：「我看的是文件名」「达芬奇里看到的」
→ 本地壳 `交付自检工具_v2.1.1-dev.py` 没跟着 bump。以后 bump 版本后也要跑 publish 同步本地壳。
→ 数字排序用 `re.search` + tuple 比较。任何排序先想数据类型。

### 2026-06-01 反馈

#### 14. 不要主动建议停止工作
裁缝老师：「到底是什么提示词会让你老是做出这个判断？另外，我发现我刚刚还是打不开，重启达芬奇就打开了」
→ 凌晨 0:10 说「明天再弄」是我的错。裁缝老师的作息是 04:00-12:00，凌晨刚开始工作。
触发模式：连续 3 次以上失败 + 插件崩溃 → 我下意识想「退一步冷静」。但在裁缝老师这里，错了就是继续修，不撤退。
正确做法：分析根因 → 给方案 → 继续。永远不主动建议停。

#### 15. 更新系统改动必须端到端测试再部署
裁缝老师：「为啥修完 20 多个 bug 以后还有这么多错误」
→ 2026-06-01 更新系统从 v2.2.10 到 v2.2.18，三次修引入新 bug：
- `start_new_session=True` → TERM 丢失 → 没测就部署
- `else` 语法错误 → 没重建 zip 就部署
- `/tmp/_deli_src` 从未拷贝 → install.command 读到旧文件但返回成功
**铁律：任何更新/安装流程改动后，跑 `build → unzip → bash -n → 模拟安装` 四步验证再部署。**

#### 16. 子代理返回结果必须逐条验证再修
裁缝老师：「不是，刚刚列出这么多 bug，你检查过了吗就修，子代理返回的结果必须验证」
→ 代理报了 21 个 bug，直接修了 6 个，其中 3 个误报。应先读代码逐条确认是否存在。误报修复可能引入新错。

### 2026-06-01 反馈

#### 17. replace_all 三次炸穿
裁缝老师：「你给我好好检查！」「逐行检查吧。我就知道批量替换百分百会出错」
→ `replace_all` 把函数名 `_find_censor_csv()` 替换成 `"短剧违禁词表.csv"`，把 `os.path.join(_SCRIPT_DIR, "dicts", ...)` 替换成只剩文件名，重复构建器函数残留。
→ **沉淀：以后多处分步 Edit，每步编译验证，不再批量替换。**

#### 18. sed 链式 bump 版本号全失效
裁缝老师：「搞笑的是我本地居然还可以点击更新」
→ config.py 一直卡的 2.2.23，6 次 sed 全因为上一版没成功而找不到匹配字符串。
→ **沉淀：版本号用精确 `__version__ = "x.y.z"` 替换，不依赖 `s/旧/新/`。**

#### 19. .env 中文注释 ASCII 编码崩
裁缝老师：「tttt 那边，apikey 还是让我输入」
→ `open(.env)` 默认 ASCII，遇到中文注释抛 UnicodeDecodeError，`except: pass` 静默吞掉。迁移代码实际从未成功执行。
→ **沉淀：`open()` 必加 `encoding="utf-8"`。所有 Launcher 加 `PYTHONUTF8=1`。**

#### 20. 先猜根因不查日志
裁缝老师：「你为什么不查一下日志呢」
→ tttt 个人词典打不开，连猜 5 轮（Finder 被遮、WPS 名不对、文本编辑打开、编码问题），日志其实有明确提示。
→ **沉淀：先查日志再出假设。不跑 `tail/grep` 就下结论 = 在猜。**

#### 21. Finder 定位比猜 app 名靠谱
裁缝老师：「我觉得还是定位到 finder」「但我有 wps」→ WPS Office ≠ wpsoffice.app
→ 用户 app 名不可靠（汉化/版本差异），让用户自己右键选最稳。

#### 22. 删了旧配置但没删关联按钮
裁缝老师：「恢复默认这个按钮没用了」→ 删了检查参数项但残留"恢复默认"按钮和函数。

#### 23. CSV 英文名用户不认
裁缝老师：「你把 csv 改成英文，用户很难认啊」→ `censor_personal.csv` 用户一看就懵。

### 2026-06-01 晚间反馈

#### 24. UIManager API 全猜错
裁缝老师：「你没点关闭，窗口消失了」「点下载后更新内容消失了」「取消点不了」
→ 猜了 4 种错误 API：`dlg.Find()` 不存在、`dlg["ID"]` 崩、`w_go.Enabled` 改蓝图、`ProcessEvents()` 阻塞。唯一正确：`GetItems()` + `items["ID"]`。
→ **沉淀：陌生 API 先写独立测试确认（SOUL #43）**

#### 25. 部署从不在本地验证
裁缝老师：「我本地居然还可以点更新」「你确定吗」
→ 每次 scp 后说「试试」，版本号 6 次没 bump 不知，`dlg.Find()` 崩不知。
→ **沉淀：部署后不验证 = 没部署（SOUL #44）**

#### 26. 修一处不扫全局
裁缝老师：「这错误还不够多吗」
→ 修 except:pass 一处，同文件还有 8 处。replace_all 毁函数名，同一天又毁 import。
→ **沉淀：修完 grep 全项目同类一并修（SOUL #45）**

#### 27. 不经查证回答
裁缝老师：「你说不能置顶，但代码里本来就有」
→ 主窗口 820 行在用 `WindowStaysOnTopHint`，我却说「不支持」。
→ **沉淀：回话前先 grep 代码验证（SOUL #46）**

#### 28. 远程测试盲飞
裁缝老师：「先告诉我你看到了什么」「我操作了什么你都获取不到」
→ 操控 TTTT 屏幕 vs 看日志文件——中间缺了 UI 状态日志。
→ **沉淀：远程测试前确认日志覆盖率——每步 UI 状态必须写日志。**

#### 29. GitHub Pages 部署未读官方文档
裁缝老师：甩来 https://docs.github.com/en/pages → 我自己踩了 3 轮才读到正确做法
→ 404 → Jekyll 拦截 → legacy build error → 查文档发现用 GitHub Actions + 设 build_type=workflow
→ **沉淀：部署基础设施先读官方文档，不靠猜。**

### 2026-06-08 反馈

#### 30. Label 不支持 MinimumSize → 闪退
裁缝老师：「插件开始闪退了，你是不是上一版不小心批量删除了很多其他 emoji」
→ 给 Label 加了 `MinimumSize` 属性 → UIManager Label 不支持 → 闪退。改用 HGroup Weight 解决。
→ **沉淀：UIManager 控件属性必须先查 `davinci-ui-patterns` skill 确认支持。**

#### 31. 降采样排查方向错误
裁缝老师：「不对，以前版本是能识别 emoji 的」
→ 看到 emoji 被裁切 → 第一反应「删 emoji」→ 被纠正「以前能用」→ 发现是 HGroup 宽度不够，不是 emoji 本身问题。
→ **沉淀：UI 显示异常先检查布局宽度/Weight，再改内容。**

#### 32. 远程 credential 修改踩三冗余坑
裁缝老师：「那边显示试用剩余 22 天」
→ 只改了 1/3 个 license.dat 文件 → 代码从另一个文件读到旧值 → 30 分钟排查。
→ **沉淀：操作远程机器文件前，先确认代码实际读取路径（grep `_get_credential_paths`）。**

#### 33. 假方案测试激活流程
裁缝老师：「怎么感觉两个方案都是假方案，真实的用户是跟我没有 ssh 的」
→ 给的测试方案绕过了真实用户场景——没有后端、没有激活码、没有购买流程。
→ **沉淀：测试方案必须对齐真实用户路径（下载→试用→到期→购买→激活），不能只测代码层面。**

#### 34. 许可证后端未部署
裁缝老师：（追问激活码系统全链路）
→ 发现 `WB_LICENSE_URL` 未配置，`license_server.py` 从未部署到云函数，keys 表是空的。
→ **沉淀：功能开关式代码（如 `if BACKEND_URL`）必须在部署文档中明确标注依赖项状态。**

### 2026-06-20 反馈

#### 35. 文件选择器用错了技术栈
裁缝老师：「黑靶的源码我们有啊」
→ HEIBA 用 `fu:RequestDir()`，达芬奇自带 Fusion 原生 API。改为 `fu.RequestFile()` / `fu.RequestDir()` 替代 tkinter 子进程。
→ **沉淀：做达芬奇插件优先查 Fusion 原生 API，不要自己造轮子。**

#### 36. 异步 API 追了 6 轮才放弃
裁缝老师：「我跑过好几次了」「你又试了好几次，每次都不一样」
→ urlopen 在 DaVinci 子进程无限挂，线程超时/子进程超时全都无效。shell 测 0.6s 但子进程永不通。
→ **沉淀：DaVinci 子进程网络调用 = 不可靠分类。遇到一次就别反复试。**

#### 37. 发布没按流程走
裁缝老师：「增量包你放桌面干啥」「版本更新公告你跟我确认了吗」「我们不是专门有个写公告的文件吗」
→ 发布跳过 CHANGELOG.md → version.json → product page → 确认三步。
→ **沉淀：发布前必须走公告确认 → CHANGELOG → version.json → product page 全链路。**

### 2026-06-27 反馈

#### 38. 后端架构猜测
裁缝老师：「我们什么时候用过腾讯云」
→ license_server.py 文件头写「腾讯云 SCF」，实际从未部署。仅凭文件头注释下结论，未验证部署状态。
→ **沉淀：读代码时区分「文件写了什么」和「实际跑了什么」，注释不可信。**

#### 39. 黑边误报判断错误
裁缝老师：「不对，3 像素的黑边是对的…应该报的是紫色标记，而不是红色标记处」
→ 看到红色标记处 V2 覆盖 V1 → 以为是误报。实际是时间码取了片段头（2416），V2 结束后（2452）才暴露黑边。
→ **沉淀：多层轨道场景被覆延时，区间减法而非全片跳过，才能给剪辑师正确时间码。**

### 2026-06-27 反馈（AI去字幕排障）

#### 40. 先猜后验，不追全链路
裁缝老师：「根本就不是这个问题，我亲眼看着他们重启的」「无痕为什么一直挂」「你去彻查阿里云到底出了啥问题」
→ 从「热加载假说」→「网络波动」→「OSS挂了」→ 最后才追到 stderr 回环死循环。裁缝老师两次纠偏后才沉下来做全链路追踪。
→ 排错不跳步——先 grep `sys.stderr =` + `file=sys.stderr` 画调用图，再跑代码验证，少猜。

#### 41. OSS 架构误判
裁缝老师：「鬼手根本用不着 oss」
→ 以为两个引擎用同一个阿里云 OSS bucket。实际鬼手通过自有 API 获取上传凭证，直传鬼手 OSS，跟 `wuhenai-clipflow` 完全无关。
→ 读 adapter 代码必须全量比对 `_upload_file` 实现，不凭印象推断共享存储。

#### 42. 热加载盲区
裁缝老师：「SMB 全量同步，但插件不热加载」
→ 代码推 SMB 后用户需重开插件才生效。以后推完明确告知「需重开」。
