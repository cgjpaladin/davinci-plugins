# shared/ 目录净化方案

> 2026-07-16 | 批量命名工具已执行，其他产品待跟进

## 问题

`shared/` 目录名为"共享"，但 42 个 .py 文件中：
- 仅 19% 真正被多个产品使用
- 29% 是单产品专用（放错了地方）
- 14% 完全无人使用
- 36% 仅被两个产品使用（多含"products + tools"的伪共享）

PyInstaller 构建时会把整个 `shared/` 打包进 app，导致 DMG/EXE 携带大量死文件。

## 原则

**目录结构就是 manifest。** 一个文件放在 `shared/` 里，必须被至少两个独立产品使用。
不属于此条件的，搬回各自产品目录。

## 已执行：批量命名工具

**从 shared/ 搬出（批量命名+创壹 = 同一产品族）：**
- `app_core.py` → `批量命名工具/app_core.py`
- `naming.py` → `批量命名工具/naming.py`
- `naming_checks.py` → `批量命名工具/naming_checks.py`

**保留在 shared/（真正跨产品共享）：**
| 文件 | 使用者 |
|------|------|
| `_qr.py` | 批量命名 + 交付自检 |
| `license.py` | 批量命名(指纹) + 交付自检(授权) |
| `updater.py` | 批量命名 + 交付自检 |
| `update_config.py` | updater 的配置 |

**效果：**
- DMG shared/ 从 42 文件 → 4 文件
- Delta 从 26 文件 → 11 文件（含新旧路径兼容副本）
- 不需要任何构建时 hack

## 待执行：其他产品

### AI去字幕

从 shared/ 搬出（仅 AI去字幕 使用）：
- `core.py` `pricing.py` `pricing_defaults.py`
- `pipeline_base.py` `pipeline_utils.py` `pipeline_log.py`
- `http_fallback.py` `ops_logger.py`
- `brand_template.py` `subtitle_state.py` `srt.py`
- `mappings.py` `llm_providers.py` `script_parser.py`
- `ledger.py` `log_writer.py`

### 交付自检工具

从 shared/ 搬出（仅交付自检使用）：
- `secure_store.py` `product_registry.py`
- `tk_dialogs.py` `llm_typo_check.py` `script_parser.py`

### 创壹特供版

搬出：`naming_createone.py`

### 可直接删除（零使用）

- `_write_env.py` `launcher_router.py`
- `render_utils.py` `xml_utils.py`
- `cross_platform.py`（open_in_explorer 零引用）
- `interface.py` `logger.py` `env.py`
- 子包：`pylut/` `dftt_timecode/` `pypdf/` `ui/` `utils/`

### 保留在 shared/（真共享）

- `deploy_config.py` — AI去字幕 + 交付自检 + tools
- `fusionscript_loader.py` — AI去字幕 + 交付自检 + tools
- `timecode.py` — AI去字幕 + 交付自检 + tools
- `camera_detect.py` `resolution.py` `macos_utils.py` — 达芬奇通用
- 以上 `_qr.py` `license.py` `updater.py` `update_config.py`

## 执行方法（模板）

1. `mv shared/xxx.py 产品目录/`
2. 搜索所有 `from shared.xxx` / `import shared.xxx` → 改为本地 import
3. 如果该文件还被其他产品引用 → 不搬，留在 shared/
4. 更新构建脚本（如有）中的文件路径
5. 预检 → 构建 → 验证
