# 达芬奇插件工坊 — 防盗版技术方案

> 2026-05-15 | License Key 系统架构 + 代码保护 + 反破解策略
>
> **核心原则**：Python 无法做到绝对防盗。目标不是防住专业逆向工程师（你防不住），而是让"普通用户花钱买"比"找盗版/破解"更容易。

---

## 一、防到什么程度：三个级别

| 级别 | 防谁 | 手段 | 投入 |
|:----:|------|------|:--:|
| **L1 - 基础** | 普通用户之间互相拷贝 | License Key + 在线激活 | 2天 |
| **L2 - 中等** | 有点技术但不专业的"资源分享者" | L1 + 硬件绑定 + 代码混淆 | 1周 |
| **L3 - 硬核** | 专业逆向工程师 | L2 + C扩展 + 服务端核心逻辑 | 2-4周+ |

**你的选择**：先做到 **L2**。比 L1 多花 5 天，挡住 95% 的白嫖用户。达到 L3 的投入不值得——你的目标用户不是逆向工程师。

---

## 二、License Key 系统设计

### 2.1 整体流程

```
用户购买
    ↓
服务器生成 License Key
    ↓
用户拿到 Key → 运行 activate.sh → 输入 Key
    ↓
客户端发送 (Key + 机器指纹) → 服务器验证
    ↓
服务器记录激活 → 返回签名 Token
    ↓
客户端保存 Token 到本地 → 插件启动时验证 Token
    ↓
每次启动：本地验证 Token（离线 OK）
    ↓
每 30 天：必须联网刷新 Token（防止离线永用）
```

### 2.2 License Key 格式

```
DAVINCI-AIWS-XXXX-XXXX-XXXX
  │      │      └────────── 随机码（12位，字母数字）
  │      └── 产品代码（AIWS=去字幕, DELC=自检, RNMR=命名工具, ALL=全套）
  └── 品牌前缀
```

示例：
- AI去字幕个人版：`DAVINCI-AIWS-PERS-A3K9-M7X2`
- 全套团队版：`DAVINCI-ALL-TEAM-B8F1-Q2L6`

### 2.3 机器指纹

用多个硬件标识组合生成唯一指纹：

```python
import hashlib
import subprocess
import platform

def machine_fingerprint() -> str:
    """生成当前机器的唯一指纹"""
    parts = []
    
    # macOS: 硬件 UUID（最稳定的标识）
    if platform.system() == "Darwin":
        r = subprocess.run(
            ["ioreg", "-d2", "-c", "IOPlatformExpertDevice"],
            capture_output=True, text=True
        )
        for line in r.stdout.split("\n"):
            if "IOPlatformUUID" in line:
                parts.append(line.strip())
                break
    
    # 备选：序列号
    r = subprocess.run(
        ["system_profiler", "SPHardwareDataType"],
        capture_output=True, text=True
    )
    for line in r.stdout.split("\n"):
        if "Serial Number" in line:
            parts.append(line.strip())
            break
    
    # MAC 地址（作为辅助）
    import uuid
    parts.append(str(uuid.getnode()))
    
    raw = "|".join(parts)
    return hashlib.sha256(raw.encode()).hexdigest()[:16]
```

**为什么用 SHA256 前 16 位？**
- 完整指纹太长，用户看不懂
- 16 位 hex 碰撞概率极低（16^16 ≈ 1.8×10^19）
- 换硬盘/网卡不影响（优先用 UUID）

### 2.4 激活协议

```python
# ============================================
# 客户端: activate.py（随插件分发）
# ============================================

import json, os, hashlib, urllib.request

ACTIVATION_SERVER = "https://license.davinci-plugins.com"  # 你的服务器
ACTIVATION_FILE = os.path.expanduser("~/达芬奇插件工坊/.activation")

def activate(license_key: str) -> dict:
    """激活 License Key，返回激活结果"""
    fingerprint = machine_fingerprint()
    
    payload = json.dumps({
        "key": license_key,
        "fingerprint": fingerprint,
        "hostname": platform.node(),
        "product": "ai_subtitle",
        "version": "1.11.3",
    }).encode()
    
    req = urllib.request.Request(
        f"{ACTIVATION_SERVER}/api/v1/activate",
        data=payload,
        headers={"Content-Type": "application/json"}
    )
    
    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            result = json.loads(resp.read())
    except Exception as e:
        return {"ok": False, "error": f"激活服务器连接失败: {e}"}
    
    if result.get("ok"):
        # 保存 Token 到本地
        os.makedirs(os.path.dirname(ACTIVATION_FILE), exist_ok=True)
        with open(ACTIVATION_FILE, "w") as f:
            json.dump({
                "token": result["token"],
                "product": result["product"],
                "plan": result["plan"],
                "activated_at": result["activated_at"],
                "expires_at": result.get("expires_at"),
            }, f)
        os.chmod(ACTIVATION_FILE, 0o600)  # 只有自己能读
    
    return result


# ============================================
# 客户端: license.py（每次启动时调用）
# ============================================

import time

def check_license() -> dict:
    """检查本机授权状态。返回 {"ok": True/False, ...}"""
    
    if not os.path.exists(ACTIVATION_FILE):
        return {"ok": False, "reason": "未激活", "code": "NOT_ACTIVATED"}
    
    try:
        with open(ACTIVATION_FILE) as f:
            data = json.load(f)
    except Exception:
        return {"ok": False, "reason": "激活文件损坏", "code": "CORRUPTED"}
    
    # 1. 验证 Token 签名（本地，离线 OK）
    token = data.get("token", "")
    if not _verify_token_signature(token):
        return {"ok": False, "reason": "Token 无效", "code": "INVALID_TOKEN"}
    
    # 2. 验证机器指纹是否匹配
    expected_fp = _extract_fingerprint_from_token(token)
    if expected_fp and expected_fp != machine_fingerprint():
        return {"ok": False, "reason": "授权已绑定到其他电脑", "code": "MACHINE_MISMATCH"}
    
    # 3. 检查是否需要联网刷新（每 30 天一次）
    last_refresh = data.get("last_refresh", data.get("activated_at", 0))
    if time.time() - last_refresh > 30 * 86400:
        refresh_result = _refresh_token(token)
        if not refresh_result.get("ok"):
            return {"ok": False, "reason": "授权需联网刷新", "code": "REFRESH_REQUIRED"}
        data["last_refresh"] = time.time()
        with open(ACTIVATION_FILE, "w") as f:
            json.dump(data, f)
    
    # 4. 检查到期（如果有到期时间）
    expires = data.get("expires_at")
    if expires and time.time() > expires:
        return {"ok": False, "reason": "授权已过期", "code": "EXPIRED"}
    
    return {"ok": True, "plan": data.get("plan"), "product": data.get("product")}


def _verify_token_signature(token: str) -> bool:
    """验证 Token 的 HMAC 签名。防伪造 Token。"""
    # Token 格式: base64(payload).base64(hmac_sha256(payload, server_secret))
    try:
        import base64
        payload_b64, sig_b64 = token.rsplit(".", 1)
        # 本地验证：用公钥验证签名（RSA）或 HMAC（简单场景）
        # 这里用简化版：Token 自带 checksum
        return True  # 实际实现需要验签
    except Exception:
        return False


def _extract_fingerprint_from_token(token: str) -> str:
    """从 Token 中提取绑定的机器指纹"""
    # 解码 Token payload，提取 fp 字段
    return ""  # 实际实现


def _refresh_token(token: str) -> dict:
    """向服务器刷新 Token"""
    # 30 天联网一次，验证 Key 未被吊销
    return {"ok": True}
```

### 2.5 服务器端（最小实现）

```python
# license_server.py — 用 bottle 或 Flask 实现
# 部署到 Vercel / Cloudflare Workers / 阿里云函数均可

SECRET_KEY = os.environ["LICENSE_SECRET_KEY"]  # 不要硬编码

# 数据库（SQLite 或 KV 存储）
# licenses 表：key, product, plan, max_machines, created_at, revoked
# activations 表：key, fingerprint, hostname, activated_at, last_seen

@app.post("/api/v1/activate")
def activate():
    data = request.json
    key = data["key"]
    fingerprint = data["fingerprint"]
    
    # 1. 查 License Key 是否有效
    lic = db.get_license(key)
    if not lic or lic["revoked"]:
        return {"ok": False, "error": "无效的 License Key"}
    
    # 2. 检查已激活机器数
    activations = db.get_activations(key)
    if len(activations) >= lic["max_machines"]:
        # 如果当前机器已激活过（允许重装），不算新机器
        if fingerprint not in [a["fingerprint"] for a in activations]:
            return {"ok": False, "error": "授权设备数已达上限"}
    
    # 3. 生成 Token（含指纹绑定）
    token = generate_signed_token({
        "key": key,
        "fp": fingerprint,
        "product": lic["product"],
        "plan": lic["plan"],
        "iat": int(time.time()),
        "exp": int(time.time()) + 365 * 86400,  # Token 有效期 1 年
    })
    
    # 4. 记录激活
    db.record_activation(key, fingerprint, data.get("hostname", ""))
    
    return {
        "ok": True,
        "token": token,
        "product": lic["product"],
        "plan": lic["plan"],
        "activated_at": int(time.time()),
    }
```

### 2.6 集成到现有插件

在你的 launcher.py 或 ui.py 入口处，加一行：

```python
# launcher.py 或 stable_ui.py 的最开头
import sys, os

# 开发机跳过授权检查
import socket
_DEV_HOSTS = {"BryandeMac-mini.local", "BryandeMac-mini"}
if socket.gethostname() not in _DEV_HOSTS:
    sys.path.insert(0, os.path.dirname(__file__))
    from license import check_license
    result = check_license()
    if not result["ok"]:
        # 弹窗提示，不启动插件
        import tkinter.messagebox as mb
        mb.showerror("授权检查失败", 
            f"无法启动：{result['reason']}\n\n"
            f"错误码：{result['code']}\n"
            f"请运行 tools/activate.sh 激活授权。")
        sys.exit(1)
```

---

## 三、代码保护：防"看完代码自己改改就能用"

### 3.1 当前状态

你的插件是**纯 Python 源码**（`.py` 文件）。任何人拿到 `stable_ui.py` 就能：
- 看懂全部逻辑
- 删掉 `check_license()` 那行
- 改掉 `PRODUCT_NAME` 换个名字卖

### 3.2 可选方案

| 方案 | 防护力 | 兼容性风险 | 适合 |
|------|:-----:|:--:|------|
| **PyArmor** | ⭐⭐⭐ | 低 | Python 脚本混淆 |
| **PyInstaller 打包** | ⭐⭐ | 极高（达芬奇内运行不了） | ❌ 不适合 |
| **Cython 编译** | ⭐⭐⭐⭐ | 中（需编译对应平台 .so） | 核心模块 |
| **服务端核心** | ⭐⭐⭐⭐⭐ | 无 | 把关键逻辑放 API 里 |

### 3.3 推荐方案：PyArmor 混淆 + Cython 核心

**第一步：PyArmor 混淆（保护全部 .py 文件）**

```bash
pip install pyarmor
pyarmor gen -O dist/AI去字幕/ AI去字幕/*.py shared/*.py
```

PyArmor 把 Python 源码变成加密字节码，直接读 `.py` 文件看到的是乱码。运行时动态解密。

**注意**：达芬奇内嵌的 Python 3.13 需要兼容。先在一台灰度机上测试。

**第二步：核心 License 检查逻辑用 Cython 编译**

```python
# shared/_license_core.pyx  (Cython 源码)
# 编译成 shared/_license_core.cpython-313-darwin.so

def verify_token(token: str, secret: bytes) -> bool:
    """用 C 实现的 HMAC 验证，反编译难度高"""
    import hmac, hashlib
    # ...
```

```bash
# 编译
cython -3 shared/_license_core.pyx
gcc -shared -o _license_core.cpython-313-darwin.so \
    -I/Library/Frameworks/Python.framework/Versions/3.13/include/python3.13 \
    _license_core.c
```

**效果**：破解者拿到的是混淆过的 .py + 编译过的 .so，想绕过 License 检查需要逆向二进制，门槛高很多。

### 3.4 什么不需要保护

以下代码**不值得**花精力保护：

- `config.py`：配置而已
- `ui.py` / `ui_widgets.py`：UI 代码，抄去也没用（和你的设计系统绑定）
- `launcher.py`：启动逻辑，公开也无妨
- `pricing.py`：定价逻辑，透明反而是好事

**优先保护**：
- `pipeline_base.py` / `pipeline.py`：核心流水线
- `adapter/`：供应商适配器（含签名算法）
- `license.py`：授权验证模块

---

## 四、反破解策略（心理战）

### 4.1 定价锚定：让买比破解划算

```
AI去字幕 ¥199 ÷ 365 天 = ¥0.55/天
```

在定价页和产品里反复强化这个心理账。一天五毛五，破解的时间成本都不止这个钱。

### 4.2 联网价值：让正版用户觉得"值"

- **自动更新**：正版用户点一下升级，盗版用户手动找新版
- **API Key 管理**：正版自带 ¥100 API 额度 + 一键充值，盗版需要自己注册 API
- **技术支持**：出问题有人管
- **新功能优先**：Beta 功能正版用户先用

### 4.3 水印/审计：追责威慑

在 AI去字幕 输出结果中嵌入隐性水印：

```python
# 在视频文件的元数据中写入 License Key 哈希
# 不影响画质，但可以用来追踪泄露源
def embed_watermark(output_path: str, license_hash: str):
    """在 MP4 的 udta 原子中写入授权标识"""
    # 用 ffmpeg 或 mutagen 写入自定义元数据
```

如果有人把付费版共享出去，处理过的视频里会有他的 License 指纹。用来事后追责。

### 4.4 真·免费版：降低盗版动机

与其让用户去找破解版，不如主动提供功能受限的免费版：

```
免费版 = 完整代码 + 完整 UI + License 限制
```

- 用户能体验全部功能
- 月处理量用完了自然想买
- 不需要"找破解版"——正版已经在这了，就差付个钱

---

## 五、实施路线图

| 阶段 | 产出 | 耗时 |
|:----:|------|:--:|
| **Week 1** | License Key 生成器 + 本地激活验证（`license.py`） | 2 天 |
| **Week 1** | 激活服务器（bottle + SQLite，部署到 Vercel） | 1 天 |
| **Week 2** | PyArmor 混淆测试（达芬奇内验证兼容性） | 1 天 |
| **Week 2** | install.sh + activate.sh 用户脚本 | 1 天 |
| **Week 3** | 灰度测试：找 2-3 个同事用正式激活流程走一遍 | 2 天 |
| **Week 3** | 面包多上架 + 自动发 Key 对接 | 1 天 |
| **Week 4** | 上线卖 | — |

---

## 六、不要做的事

- ❌ **不要用在线 DRM 每次启动都联网。** 剪辑师的机器经常没网（片场、隔离环境），每次启动联网 = 体验灾难。30 天离线 + 定期刷新就够了。
- ❌ **不要做复杂的反调试。** Python 的反调试很容易被绕过，而且容易误伤正常用户（杀毒软件误报）。
- ❌ **不要花超过 2 周在防盗版上。** 你的目标用户是剪辑师，不是黑客。L2 级别 2 周搞定，剩下精力应该花在产品和销售上。
- ❌ **不要把 License 逻辑放在 SMB 共享目录里。** 你的 deploy.json 指到 SMB，但 `.activation` 文件应该放在本地 `~/达芬奇插件工坊/`。
- ❌ **不要用硬件序列号做唯一指纹。** Mac mini 集群可能有相同配置。用 UUID + MAC 组合。

---

*配套文档：`销售与分发方案.md`*
