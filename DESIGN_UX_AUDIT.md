# 批量命名工具 · 表格版 — UX 审查报告

> ArchitectUX | 2026-05-24 | 基于 Nielsen 十大启发式，今日完整代码审查后

---

## 设计背景

- **受众**：剪辑总监 + 20 剪辑师，每天处理几十上百个视频素材
- **环境**：暗色剪辑机房，WKWebView 桌面应用
- **核心任务**：拖文件 → 填元数据 → 一键归档/重命名
- **设计要求**："闭着眼睛点"——熟练用户应零思考

---

## 启发式评分

| # | 启发式 | 分 | 说明 |
|---|--------|-----|------|
| 1 | 系统状态可见 | **2/4** | 完成度色条好；操作结果吐司消失 |
| 2 | 与现实匹配 | **3/4** | 表头已中文化；Ep/Sc/Gr 内部名称全行业通用 |
| 3 | 用户自由控制 | **2/4** | 重命名有 Ctrl+Z；归档刚加撤销；无"确认前预览" |
| 4 | 一致性与标准 | **2/4** | 两端维护两套 UI；列齐 vs 卡片焦点模型不一致 |
| 5 | 错误预防 | **3/4** | 数字验证 + 归档路径碰撞检测 + SHA256 去重 |
| 6 | 识别而非回忆 | **2/4** | 空单元格红底很直观；method→desc 联动完全不可见 |
| 7 | 灵活高效 | **2/4** | 逐行双击是唯一的填法；无批量设 Ep/Sc |
| 8 | 美学与极简 | **3/4** | 暗色统一；无 AI 审美痕迹；表格本身干净 |
| 9 | 帮助用户识别错误 | **2/4** | 归档失败信息随吐司消失；无持久错误面板 |
| 10 | 帮助文档 | **0/4** | 无。表头中文是唯一文档 |

**总分：21/40 — 中低。有骨架，缺血肉。**

---

## 逐项剖析

### ① 系统状态可见 (2/4)

**好的**：
- 行完成度圆点 (绿/黄/红) ——今日刚做
- 空单元格暗红底 + "—" ——一眼看出缺什么
- 状态栏 "N/M 就绪" ——实时计数

**坏的**：
- 操作结果是一次性吐司，2.5 秒消失。归档完 10 个文件后，用户看到的界面和归档前一模一样——没有"刚才发生了什么"的任何痕迹
- 归档成功但 UI 不反映——行变灰是今天才加的，之前归档完文件还在列表里但源路径已失效
- 无进度条——大量文件归档时用户只能干等

### ② 用户自由控制 (2/4)

**好的**：
- 重命名有 Ctrl+Z 撤销栈
- 归档刚加上撤销（删除 SMB 副本 + 恢复行）

**坏的**：
- 没有"重命名前预览"——确认对话框只显示第一个文件名，用户点"确认"时其实不知道所有文件会变成什么样
- 归档也没有逐文件预览，只有一个路径模板

### ④ 一致性与标准 (2/4)

**好的**：
- 字段编辑器行为一致——双击激活，Enter 提交，Esc 取消
- 颜色系统统一（green/yellow/red 三色语义）

**坏的**：
- 表格版和卡片版是两个完全不同的代码库，同一个 Python 后端，但 JS 各自维护
- 卡片版有 Inspector 面板，表格版用行内编辑——新用户从卡片版切到表格版完全懵

### ⑥ 识别而非回忆 (2/4)

**坏的（关键）**：
- method→desc 联动完全不可见。用户选"双轨版"后，desc 列自动变成下拉框或锁定值——但没有动画、没有高亮、没有提示。desc 改动后用户可能根本没注意到
- TK 列的显示值和归档时的实际值可能不同——`buildTK(i)` 是在 JS 端基于列表位置计算的，归档时 Python 端会重新扫描 SMB 文件夹。如果用户先归档一批再拖入一批，TK 预览和实际可能不一致

### ⑦ 灵活高效 (2/4) — **最痛的点**

**当前**：Ep/Sc 是所有行的必填字段，但必须逐行双击。18 行 = 36 次双击。

**为什么这比看起来更严重**：剪辑师的工作流是"所有素材同一个 Ep/Sc，只是 desc/method 不同"。把 Ep/Sc 填 18 次是纯粹的体力劳动，不是任何人的编辑工作。

---

## 即时可修项 (今日可完成)

| # | 问题 | 方案 |
|---|------|------|
| A | method→desc 联动不可见 | 选 method 后 desc 单元格短暂高亮（0.3s 黄色脉冲） |
| B | 确认对话框信息不足 | 归档对话框：展示前 3 个文件的完整目标路径 |

## 中期工程 (下次迭代)

| # | 问题 | 方案 |
|---|------|------|
| C | 逐行填 Ep/Sc 太慢 | 批设栏：表头上方一行输入框，填入后 "应用到全部空行" |
| D | 操作结果消失 | 持久结果面板替代吐司，底部一行可堆叠 |
| E | 无文件名预览 | 选中行后底部预览条：`完整文件名 → 归档路径` |
| F | 卡片版停用 | 从桌面上移除，JS/CSS 保留但不构建 |

---

*ArchitectUX | 2026-05-24*
