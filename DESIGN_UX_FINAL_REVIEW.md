# 表格版 — UX 终审

> ArchitectUX | 2026-05-24 | 基于全量代码审查 + 今日会话回溯

---

## 评分

| 维度 | 分 | 评语 |
|------|-----|------|
| 视觉层次 | 4/5 | 暗色统一，层级分明，无 AI 审美痕迹 |
| 交互效率 | 3/5 | 双击编辑流畅；缺列批量填 |
| 状态反馈 | 4/5 | 完成度色条+红格+持久结果——今天全补上了 |
| 错误预防 | 4/5 | 数字验证+SHA256去重+路径碰撞——扎实 |
| 韧性 | 3/5 | 字段解析稳健；WKWebView 双火已根治 |
| 美学 | 5/5 | Linear dark 风格，剪片房的工具不该花哨 |

**总评：23/30 — 优秀，三件可进**。

---

## 只修这三件

### A. 列宽不够灵活

**现状**：`table-layout:fixed` 硬编码 60px/100px。中文表头"制作方式"、"镜头描述"在当前宽度下刚好，但拖宽窗口后列宽不跟随。

**修法**：`col-desc` 和 `col-method` 加 `min-width:100px`，去掉固定 `width`，靠 `table-layout:fixed` 和 `col-base:auto` 让剩余空间分配。

### B. 表格行高太紧

**现状**：`height:32px` + `padding:8px 12px`。18 行紧凑可读，但双击编辑时 input/select 换行或裁切。编辑态单元格没有预留垂直空间。

**修法**：`td.editing { padding-top:4px; padding-bottom:4px }` — 编辑时压缩 padding 给 input 腾空间。

### C. `_EMPTY_KEYS` 的意图问题

**现状**：未解析文件 ep/sc/gr 强制置空。但拿到 `Ep01_Sc02_Gr03_Tk01_xxx.mp4` 解析出 ep=01 后，又被 `_EMPTY_KEYS` 覆盖成空。逻辑是"这些字段解析值不可信"——但理由不清晰。

**修法**：`_EMPTY_KEYS` 去掉 ep/sc/gr，只留 tk。ep/sc/gr 解析值信任解析器。如果 `parse_filename` 返回值里有，就用——当前的新正则已经可靠。

---

## 不要动的事

- 不要加面板、侧栏、批设栏——表格的纯粹性就是武器
- 不要改按钮布局——三按钮（重命名/归档/撤销）层级清晰
- 不要改字体——`fs-body:10px` 在暗色环境下刚好，比卡片版的 13px 更专业
- 不要加图标替代文字——中文表头已够好

---

*ArchitectUX | 2026-05-24*
